# CozyPomo — Plan & Trạng thái dự án

> Tài liệu này theo dõi tiến độ toàn dự án ở mức task cụ thể. Cập nhật thủ công mỗi khi có thay đổi lớn — không tự động sinh. Tham chiếu: [`docs/technical-spec.md`](docs/technical-spec.md) (kiến trúc/Screen List/Function List), [`docs/setup-checklist.md`](docs/setup-checklist.md) (checklist hạ tầng), [`docs/deploy_backend.md`](docs/deploy_backend.md) (hướng dẫn deploy).
>
> Tác giả sản phẩm: Dev1002. Cập nhật lần cuối: 2026-07-26.

## Tổng quan tình trạng

| Mảng | Trạng thái | Ghi chú |
|---|---|---|
| Backend API (NestJS) | ✅ Hoàn chỉnh, đã test thật **và đã release production** | Toàn bộ endpoint theo Function List đã có + economy v2 + economy v3 (chọn 1 tiền tệ/phiên, tỉ giá 1:10) + module Debug/cheat tester-only — đã lên `backend-v1.20260723.004` |
| Trang quản trị AdminJS | ✅ Hoàn chỉnh, đã Việt hoá + brand hoá | Species/EggType có UI card riêng, có trang liệt kê API; đã thêm resource `GameSettings`/`OwnedEgg` |
| Dữ liệu seed | ✅ 175 loài + 4 loại trứng + 10 tài khoản tester | Production đã reseed lại theo economy v3 (T-090, 2026-07-23) — tỉ giá 1:10 + bonus loài đa dạng đã có hiệu lực thật, không còn kẹt giá trị cũ từ economy v2 |
| CI/CD workflows | ✅ Đã viết **và xác nhận chạy thật thành công cho cả backend lẫn Android** | `backend-v1.20260723.004` (build→push→SSH deploy) và `app-v1.20260723.002` (build→ký release→upload asset) đều chạy xanh end-to-end — T-054 (test workflow Android lần đầu) coi như xong |
| Branding (icon/favicon/logo) | ✅ Hoàn chỉnh, đã deploy | `docs/branding/`, hiện trên `/admin` thật |
| Backend production | ✅ **Đang chạy thật** tại `https://cozyapi.nhutnm.id.vn`, đã lên `backend-v1.20260726.001` (Chợ + Egg economy 7 loại trứng, đã reseed) | Domain + SSL + Reverse Proxy + Docker trên aaPanel, migration tự chạy qua Docker `CMD`, đã verify `GET egg-types` trả đủ 7 loại sau reseed (T-118) |
| Android app | ✅ 10/10 màn + màn mới Giới thiệu (T-102) + Chợ/Khu vườn (T-109/T-113), bottom nav 4 tab (Cửa hàng chuyển vào FAB, T-111) | T-029→T-031, T-040, T-041, T-069, T-071→T-089, T-091→T-114, T-116/T-117 xong; **đã release `app-v1.20260726.001`** (`v0.8.0`, versionCode 8, T-118) lên GitHub qua CI — lần build đầu fail vì thiếu gỡ `WorkManagerInitializer`, đã sửa và release lại thành công |
| Tài liệu (technical-spec.md + wireframe) | ✅ Đã đồng bộ lại theo bản build thật (T-100, 2026-07-24) | Screen List/Function List/wireframe khớp kinh tế 2 tiền tệ, Kho đồ (S-07b), bottom nav 5 tab, S-07a đã đánh dấu bỏ khỏi phạm vi |
| Test suite (backend + Android) | ⬜ Chưa có | 0 file test ở cả 2 phía |
| Android release (keystore/secrets/Play Store) | ✅ Secrets đã cấu hình, workflow đã chạy thật thành công | `app-v1.20260724.002` build/ký/upload asset OK (T-053/T-054 xong) — còn thiếu bước đăng thật lên Play Console (Nhóm E) |
| Working tree | 🟨 Có thay đổi chưa commit (T-042, T-043, 2026-07-25) | SoundManager/NotificationManager + WorkManager outbox — build Gradle thật thành công (`assembleDebug`, KSP Room+Hilt xanh), **chưa verify trên emulator/thiết bị thật**, chờ Dev1002 xác nhận trước khi commit/push/release |

### ✅ Production đã reseed theo economy v2 (2026-07-23)

Ban đầu cố ý **không** chạy lại `prisma/seed.ts` trên production sau migration `focus_economy_v2` — script này xoá sạch `User`/`Session`/`LedgerEntry`/`OwnedEgg`/... (xem `deleteMany` đầu file), sẽ mất lịch sử 10 tài khoản tester đã tích luỹ từ T-064. Đã báo rủi ro này với Dev1002; Dev1002 xác nhận cho phép xoá vì sản phẩm còn ở giai đoạn phát triển, chưa có người dùng thật cần giữ.

- **T-070 — Reseed production qua GitHub Actions.** Thêm job `reseed-production` vào `.github/workflows/backend-deploy.yml` (khoá kép: chỉ chạy khi `workflow_dispatch` với `reseed=true` **và** `confirm_reseed` gõ đúng chữ `RESEED` — tránh bấm nhầm checkbox) thay vì dán `DEPLOY_HOST`/`DEPLOY_USER`/`DEPLOY_PATH` ra chat: job SSH vào server, cài tạm `ts-node`/`typescript` trong container `api` đang chạy (production build `npm ci --omit=dev` nên thiếu 2 gói này, giống cách làm ở T-064), rồi chạy `npx prisma db seed`. Đã dispatch thật (run [29989908364](https://github.com/nhockool1002/cozy-pomo-focus-app/actions/runs/29989908364)), job `reseed-production` xong sau 32s, job `deploy` bị skip đúng như thiết kế (không đụng tới đang chạy).
- **Đã verify lại bằng curl thật sau khi reseed:** `GET /game-settings` trả `coinsPerFocusMinute=1` (đúng seed, không còn lazy-create default), `GET /egg-types` trả đủ 4 loại với `priceHours`/`hatchDurationMin` đúng giá trị thiết kế (VD Trứng Bí Ẩn 150 Xu/150 giờ tích luỹ/300 phút ấp), đăng nhập `tester01@cozypomo.dev` / `Tester123!` (mật khẩu chung mọi tester, xem `seed.ts:224`) thành công, có số dư kép (`60 Xu Lá` / `65 phút`) và `OwnedEgg` INCUBATING thật để test egg-picker.
- **Không cần** vào AdminJS chỉnh tay nữa như dự tính ban đầu — reseed đã tự đưa toàn bộ dữ liệu về đúng thiết kế economy v2.

---

## ✅ Đã hoàn thành

### Backend (`backend/`)
- **T-001 — Scaffold NestJS + Prisma + Docker.** Cấu trúc module chuẩn Nest, kết nối Postgres qua Prisma, `docker-compose.yml` cho dev local.
- **T-002 — Auth module.** Đăng ký/đăng nhập email+mật khẩu, JWT access + refresh token, endpoint `/auth/register` `/auth/login` `/auth/refresh` `/auth/me`.
- **T-003 — Species module.** `GET /species`, `GET /species/:id`, lọc theo category/rarity — phục vụ `CollectionRepository.getCollection`/`getSpeciesDetail` phía Android.
- **T-004 — Eggs module.** `GET /egg-types`, `GET /egg-types/:id`, `GET /egg-types/:id/odds` (tỉ lệ nở) — phục vụ `EggRepository`.
- **T-005 — Sessions module.** `POST /sessions` (bắt đầu phiên), `PATCH /sessions/:id/complete`, `PATCH /sessions/:id/give-up`, `GET /sessions` — phục vụ `TimerRepository`, có random rarity-roll theo `rarity_weights`/`egg_drop_table`.
- **T-006 — Currency module.** `GET /currency/balance`, `GET /currency/ledger` — sổ giao dịch Xu Lá, cộng/trừ nội bộ khi hoàn thành phiên hoặc mua hàng.
- **T-007 — Collection module.** `GET /collection`, `GET /collection/progress`, `PATCH /collection/:speciesId/favorite`.
- **T-008 — Shop module.** `GET /shop-items`, `POST /shop-items/:id/purchase`, `GET /inventory`, `PATCH /inventory/:id/equip`.
- **T-009 — Stats module.** `GET /stats/daily`, `/stats/range`, `/stats/summary` — phục vụ màn Thống kê (S-06).
- **T-010 — Settings module.** `GET`/`PATCH /settings` — thời gian focus/break mặc định, Strict Mode, sound theme.
- **T-011 — Sync module.** `POST /sync/batch` — nhận outbox từ app khi mạng chập chờn/offline→online.
- **T-012 — Health check.** `GET /health`, không cần JWT, không bị `setGlobalPrefix` chặn — dùng để healthcheck từ ngoài.
- **T-013 — Swagger/OpenAPI.** Tự sinh tại `/docs` (UI) và `/docs-json` (raw spec) từ decorator `@ApiTags`/`@ApiBearerAuth` trong code — luôn khớp thực tế.
- **T-014 — Seed data.** `backend/prisma/seed.ts`: 175 loài (50 Thú rừng/50 Sinh vật biển/50 Thực vật/25 Thần thú SSR), 4 loại trứng + bảng tỉ lệ rơi, vật phẩm cửa hàng, 10 tài khoản tester có lịch sử phiên/Xu Lá/bộ sưu tập giả lập.
- **T-015 — Dockerfile production.** Multi-stage build, entrypoint tự chạy `prisma migrate deploy` trước khi start server — không cần thao tác tay khi deploy.

### Trang quản trị AdminJS (`backend/src/admin/`)
- **T-016 — Species List/Show tuỳ biến.** Lưới thẻ loài giống wireframe: vầng hào quang theo cấp bậc (B/A/S/SS/SSR), hiệu ứng bay bổng/xoay/chớp sáng cho SSR ("rực lửa" — quầng sáng mix-blend screen, vòng xoay 2 chiều, tia lửa đa màu), badge cấp bậc, bộ lọc theo danh mục/cấp bậc, tìm kiếm theo tên. File chính: `species-art.ts` (bộ sinh SVG + CSS hiệu ứng dùng chung), `SpeciesList.tsx`, `SpeciesShow.tsx`, `SpeciesThumbnail.tsx`.
- **T-017 — EggType List tuỳ biến.** Lưới thẻ trứng cùng ngôn ngữ hình ảnh với Species: icon trứng sinh theo `colorHex` riêng từng loại, vầng hào quang theo mốc giá (Thường/Hiếm/Huyền thoại). File: `egg-art.ts`, `EggTypeList.tsx`.
- **T-018 — Việt hoá toàn bộ giao diện AdminJS.** `admin-i18n.ts` — dịch nhãn điều hướng, nút bấm, thông báo lỗi, nhãn thuộc tính + giá trị enum cho toàn bộ 11 resource (kể cả bảng mặc định như Session/LedgerEntry), trang đăng nhập, dashboard chào mừng.
- **T-019 — Trang "API cho App".** `ApiExplorer.tsx` — custom page trong sidebar, đọc `/docs-json` để liệt kê toàn bộ endpoint (method, path, tag, có cần JWT hay không), có filter theo nhóm tính năng + tìm kiếm, nút mở Swagger UI để thử request thật.
- **T-020 — Branding trang quản trị.** Favicon + logo thật (bình ấp trứng phát sáng, đúng bảng màu chính thức) thay vì mặc định AdminJS — wiring qua `useStaticAssets` trong `main.ts` + `branding.favicon`/`branding.logo` trong `admin.module.ts`.

### Branding (`docs/branding/`)
- **T-021 — Icon mark + logo ngang.** `icon-mark.svg` (bình ấp trứng, đã kiểm tra nằm gọn safe-zone 66dp adaptive icon), `logo-horizontal.svg` (icon + wordmark "CozyPomo", font Arial Rounded MT Bold). Export sẵn: favicon multi-size, apple-touch-icon, icon 512/1024 (Play Store hi-res, opaque).
- **T-022 — Android adaptive icon thật.** Thay `ic_launcher_background.xml`/`ic_launcher_foreground.xml` placeholder bằng path data thật port từ icon mark.

### Hạ tầng / DevOps
- **T-023 — Repo GitHub + cấu trúc monorepo.** `app/` `backend/` `docs/`, đã push nhánh `main`.
- **T-024 — `.github/workflows/android-release.yml`.** Kích hoạt bởi tag `app-v*`, build AAB+APK ký sẵn.
- **T-025 — `.github/workflows/backend-deploy.yml`.** Kích hoạt bởi tag `backend-v*` hoặc chạy tay, build+push Docker image lên ghcr.io, SSH deploy lên aaPanel.
- **T-026 — Tài liệu vận hành.** `docs/setup-checklist.md`, `docs/deploy_backend.md` (hướng dẫn chi tiết từng bước deploy backend, bao gồm cả gotcha ghcr.io package visibility).

### Android scaffold (`app/`)
- **T-027 — Khung điều hướng.** Compose + Hilt + Navigation Compose, 4 tab Bottom Nav (Trang chủ/Khu rừng/Cửa hàng/Thống kê), theme đúng Brand Guide (`ui/theme/`).
- **T-028 — Tầng network.** Retrofit `ApiService`, `AuthInterceptor` tự gắn JWT, `TokenProvider`, DI qua Hilt (`data/network/di/NetworkModule.kt`).

### Deploy production — backend đang chạy thật (2026-07-23)
- **T-059 — Đẩy code thật lên GitHub.** Phát hiện `origin/main` trên GitHub trước đó chỉ có đúng commit scaffold ban đầu — 3 commit chứa toàn bộ backend/Android/CI workflow chưa từng được push. Đã push (fast-forward, không xung đột).
- **T-060 — Xử lý ghcr.io package visibility.** Image build lần đầu qua `GITHUB_TOKEN` mặc định ở chế độ Private dù repo Public, khiến `docker compose pull` trên server lỗi `denied`. Xử lý bằng `docker login ghcr.io` trên server bằng Personal Access Token.
- **T-061 — Đổi port tránh xung đột trên server.** Server đã có sẵn service khác chiếm `127.0.0.1:5432`; sau đó đổi tiếp cả cụm port để tránh xung đột khác: `api` `3000→3012`, `db` `5432→5433→5437` (host-side only, port nội bộ container không đổi). Cập nhật `docker-compose.prod.yml` + `docs/deploy_backend.md`.
- **T-062 — Cấu hình SSH deploy key.** Tạo cặp key riêng `deploy_cozypomo`, thêm public key vào `authorized_keys` trên server, xác nhận `DEPLOY_HOST`/`DEPLOY_USER`/`DEPLOY_SSH_KEY`/`DEPLOY_PATH` hoạt động đúng qua GitHub Actions (job `deploy` SSH thành công).
- **T-063 — Domain + SSL + Reverse Proxy thật.** Domain cuối cùng: `cozyapi.nhutnm.id.vn` (đổi từ `cozy_api...` do dấu gạch dưới không hợp lệ cho Let's Encrypt). DNS A record, site aaPanel, SSL Let's Encrypt, Reverse Proxy → `127.0.0.1:3012` — đã verify `curl https://cozyapi.nhutnm.id.vn/health` trả `{"status":"ok"}`.
- **T-064 — Seed dữ liệu production.** Phát hiện image production build bằng `npm ci --omit=dev` nên thiếu `ts-node`/`typescript` (cần để chạy `prisma/seed.ts`) — xử lý bằng cài tạm 2 gói đó ngay trong container trước khi `npx prisma db seed`. Đã cập nhật hướng dẫn đúng trong `docs/deploy_backend.md`.
- **T-065 — Cập nhật `API_BASE_URL`.** `app/app/build.gradle.kts` — build `release` giờ trỏ `https://cozyapi.nhutnm.id.vn/api/v1/` (build `debug` vẫn giữ `10.0.2.2:3000` cho emulator gọi backend local, không đổi).
- **T-066 — Release đầu tiên `backend-v1.20260723.001`.** Xác nhận toàn bộ pipeline `GitHub Release → build-and-push (ghcr.io) → SSH deploy (pull + up -d)` chạy tự động thành công, không cần thao tác tay trên server (sau khi sửa xong T-060 → T-062). Đây là lần đầu tiên quy trình auto-deploy hoạt động end-to-end đúng như thiết kế.
- **T-067 — Fix `Dockerfile` thiếu `COPY public/`.** Sau khi deploy, favicon/logo AdminJS bị vỡ ảnh trên production dù cấu hình đúng — do giai đoạn `runner` của `Dockerfile` chưa từng copy thư mục `public/` (chỉ copy `dist/`, `prisma/`, `src/admin/components/`) nên `useStaticAssets` không có gì để phục vụ trong container thật, dù chạy đúng khi test từ source local. Đã thêm `COPY public ./public`, build thử local xác nhận file có mặt trong image trước khi release `backend-v1.20260723.002` — đã verify `/branding/favicon.png` và `/branding/logo.png` trả `200` qua domain thật.

### Backend — Economy v2: Xu Lá + Giờ tích luỹ + ấp trứng theo bình (2026-07-23)

Thay đổi lớn theo yêu cầu sản phẩm: bỏ mô hình "1 phiên tập trung = 1 lần thử nở trứng" (roll ngay khi hoàn thành), thay bằng mô hình 2 loại tiền tệ + trứng ấp dần theo từng instance sở hữu riêng.

- **T-068 — Economy v2: schema + service + release `backend-v1.20260723.003`.**
  - **Prisma schema:** `CurrencyType` enum (`COIN`/`FOCUS_MINUTE`) gắn vào `LedgerEntry` hiện có (tái dùng 1 bảng sổ giao dịch qua cột phân loại, không tạo bảng mới) — đổi tên gọi trong sản phẩm "Xu Lá" (COIN, chi tiêu ở Shop) và "Giờ tích luỹ" (FOCUS_MINUTE, đơn vị phút, dùng mua trứng theo giờ). `EggType` thêm `priceHours`/`hatchDurationMin` (mỗi loại trứng có giá + thời gian ấp riêng, quản trị qua AdminJS). Model mới `OwnedEgg` — mỗi lần mua là 1 dòng riêng (`INCUBATING`→`HATCHED`), field `incubatedMin` cộng dồn qua nhiều phiên, tự roll loài + ghi `CollectionService` khi chạm `hatchDurationMin`. Model mới `GameSettings` (singleton, `coinsPerFocusMinute`, lazy-create nếu thiếu). `Session` bỏ hẳn `eggTypeId`/`resultSpeciesId` (roll-ngay-khi-xong), thay bằng `ownedEggId`/`incubationRatio`/`minutesAccumulated`/`minutesIncubated` — 1 phiên có thể vừa cộng Giờ tích luỹ vừa ấp trứng theo tỉ lệ người dùng chọn trước khi bắt đầu (ví dụ: 30 phút, 50% → 15 phút Giờ tích luỹ + 15 phút ấp).
  - **`SessionsService.complete()`:** tính `coinsEarned = round(plannedMin * gameSettings.coinsPerFocusMinute)`, chia `plannedMin` theo `incubationRatio` thành `minutesIncubated`/`minutesAccumulated`, ghi cả 2 dòng ledger (COIN + FOCUS_MINUTE) trong 1 transaction, gọi `OwnedEggsService.incubate()` nếu có chọn trứng.
  - **`ShopService.purchase()`:** danh mục EGG giờ trừ tiền theo `EggType.priceCoin` hoặc `priceHours` (chọn qua `payWith`) và tạo `OwnedEgg` mới — khác nhánh non-EGG cũ (vẫn `InventoryItem`, chỉ trả bằng Xu Lá).
  - **Migration không dùng `prisma migrate dev`** (cần TTY, không chạy được non-interactive trong môi trường agent) — thay bằng `prisma migrate diff --from-url <db> --to-schema-datamodel schema.prisma --script` sinh SQL tay, đặt vào `prisma/migrations/20260723065648_focus_economy_v2/`, rồi `prisma migrate deploy` (full non-interactive) để áp dụng + ghi nhận vào `_prisma_migrations`.
  - **Đã verify bằng curl thật** trên Postgres local: login, 2 số dư riêng, egg-types có field mới, owned-eggs list, session create/complete/give-up, mua bằng cả 2 loại tiền, từ chối khi không đủ tiền, gọi complete lặp lại vẫn idempotent (nhờ `clientEventId` suffix riêng cho từng ledger entry).
  - **Đã release `backend-v1.20260723.003`** lên production qua pipeline chuẩn (tag → build → push ghcr.io → SSH deploy) — **không** reseed database production (xem cảnh báo ở bảng Tổng quan).

### Android — Phase 1: Splash → Onboarding → Đăng nhập (2026-07-23)

- **T-040 — Splash (S-00) + Onboarding (S-00b) thật.** `ui/splash/SplashScreen.kt` đọc `AuthRepository` (DataStore) để quyết định điều hướng kế tiếp (Onboarding lần đầu / Login / thẳng Main). `ui/onboarding/OnboardingScreen.kt` — 3 slide `HorizontalPager`, xin quyền `POST_NOTIFICATIONS` ở slide cuối (chỉ Android 13+), đánh dấu đã xem vào DataStore nên chỉ hiện đúng 1 lần. `ui/common/JarMark.kt` — icon bình ấp trứng vẽ bằng `Canvas` (chưa có illustration asset thật ngoài icon app T-021/T-022).
- **T-041 — Đăng nhập/Đăng ký thật, tích hợp API.** `ui/login/LoginScreen.kt` + `LoginViewModel` — 1 màn toggle Đăng nhập/Đăng ký, gọi thật `POST /auth/login`/`register`, validate phía client, dịch lỗi HTTP sang tiếng Việt (`AuthErrorMapper.kt`). `data/auth/AuthRepository.kt` là nguồn sự thật: bọc `ApiService.auth/*` + `DataStore<Preferences>` (bền, qua `AuthDataStoreModule`) + `TokenProvider` (RAM, để `AuthInterceptor` đọc đồng bộ). `ui/navigation/RootNavHost.kt` nối graph cấp cao Splash→Onboarding→Login→Main (bọc ngoài `CozyPomoNavHost` 4-tab cũ). Đã test thật trên emulator (Pixel 5, API 30) với tài khoản seed `tester01@cozypomo.dev` — login trả `200`, JWT lưu bền, mở lại app vào thẳng Main.
  - **Bug đã gặp & fix:** (1) KSP2 không resolve được `AuthRepository` khi inject qua `Context.authDataStore by preferencesDataStore(...)` (extension property + delegate) — fix bằng cách cấp `DataStore<Preferences>` qua `@Provides` trong `AuthDataStoreModule` thay vì extension property. (2) Debug build gọi `http://10.0.2.2:3000` bị chặn bởi Network Security Policy mặc định (chỉ cho HTTPS) — thêm `app/src/debug/res/xml/network_security_config_debug.xml` cho phép cleartext tới `10.0.2.2`/`localhost`, chỉ áp dụng build debug (release vẫn HTTPS nguyên vẹn).
  - **Chưa làm trong T-041:** không có refresh-token flow tự động khi access token hết hạn (chỉ lưu/gửi token, chưa gọi `/auth/refresh` khi 401) — cần làm khi xây các Repository gọi API cần JWT khác ở Nhóm A. **Đã quan sát thật:** access token hết hạn sau 15 phút (`exp - iat`), lúc test T-031 số dư Xu Lá bị `401` do token cũ — phải đăng nhập lại mới gọi được API cần JWT. Cần ưu tiên làm refresh-token flow sớm trong Nhóm A, không để tới cuối.

### Android — Phase 2: Room DB + TimerRepository + Trang chủ (2026-07-23)

- **T-029 — Room DB local-first.** `data/local/session/SessionEntity.kt` + `SessionDao.kt` + `CozyPomoDatabase.kt` (`DatabaseModule.kt` cấp qua Hilt). Chỉ có bảng `sessions` ở bản đầu (đủ cho T-030) — các bảng khác (inventory, collection, settings) sẽ thêm khi xây Repository tương ứng, không tạo trước schema chưa dùng tới.
- **T-030 — TimerRepository + Foreground Service.** `data/timer/TimerRepository.kt` — `startSession`/`giveUpSession`/`completeSession`/`observeActiveSession` đúng Function List §3.1; Room là nguồn sự thật cho phiên đang chạy, backend (`POST /sessions`, `PATCH .../complete`, `PATCH .../give-up`) đồng bộ best-effort (chưa có outbox retry đầy đủ — đó là T-043). `data/timer/TimerForegroundService.kt` đếm bằng `SystemClock.elapsedRealtime()`, tự gọi `completeSession` khi về 0 dù UI không mở, cập nhật notification mỗi giây; khai báo `foregroundServiceType="specialUse"` + `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` trong `AndroidManifest.xml` (bắt buộc từ Android 14). `ensureServiceRunningIfActive()` tự khởi động lại service nếu Home mở lại và Room còn phiên RUNNING dở dang (service từng bị hệ thống kill).
- **T-031 — UI Trang chủ/Timer (S-01) thật.** `ui/home/HomeViewModel.kt` + `HomeScreen.kt` — bình ấp (`JarMark`) + nút "+" mở popup chọn trứng tối giản (chỉ tên + màu, chưa có trạng thái khoá theo sở hữu — đó là T-032 đầy đủ), đồng hồ đếm ngược lớn, slider 10–120 phút (khoá khi đang chạy), nút BẮT ĐẦU ⇄ BỎ CUỘC, dialog xác nhận bỏ cuộc, Snackbar hiện kết quả nở trứng (tên loài + Xu Lá) khi `TimerRepository.hatchEvents` bắn — thay cho hoạt ảnh nở trứng đầy đủ (T-034, chưa làm). Số dư Xu Lá đọc trực tiếp qua `GET /currency/balance` (không qua CurrencyRepository riêng — chưa cần thiết ở quy mô này).
  - **[Đã thay thế bởi T-069, xem bên dưới]** — mô tả trên là trạng thái tại thời điểm viết (giữ nguyên làm lịch sử); popup chọn trứng tối giản + Snackbar tạm đã được thay hoàn toàn bằng egg picker theo sở hữu thật + modal kết quả animate 4 nhánh.
  - **Đã test thật trên emulator** (tài khoản `tester01@cozypomo.dev`): bắt đầu phiên → `POST /sessions` trả `201` → đếm ngược đúng theo giây (đối chiếu qua notification) → chỉnh thẳng `startElapsedRealtimeMs`/`plannedMin` trong SQLite (qua `adb run-as` + `sqlite3`) để mô phỏng hết giờ mà không phải chờ thật → xác nhận Foreground Service tự gọi `completeSession`, backend roll ra loài thật ("Sứa Đêm Sâu", cấp B), Xu Lá cộng đúng (`60 → 125`, +65 = plannedMin ban đầu), UI reset về Idle + Snackbar. Test riêng luồng Bỏ cuộc: dialog cảnh báo đúng, `PATCH .../give-up` trả `200`, Xu Lá không đổi, UI reset về Idle.
  - **Sửa theo phản hồi (2026-07-23):** (1) Spinner loading trên nút Đăng nhập/Đăng ký bị lệch quỹ đạo — do `Modifier.height(20.dp)` không kèm width khiến `CircularProgressIndicator` bị méo; đổi sang `Modifier.size(20.dp)`. (2) Dòng "Chưa có tài khoản? Đăng ký" bị nhảy dòng — thêm `fillMaxWidth()` + `maxLines = 1, softWrap = false` cho Row xác nhận đăng ký/đăng nhập. (3) Thêm nút Đăng xuất ở Trang chủ (icon cạnh ⚙, gọi `AuthRepository.logout()` rồi điều hướng về Login qua `RootNavHost`) — trước đó chưa có cách nào đăng xuất. (4) ~~Trứng Bí Ẩn khoá theo tổng phút tập trung~~ — cơ chế này đã bị **thay thế hoàn toàn** bởi economy v2 (T-069): không còn khoá theo ngưỡng tích luỹ toàn cục, trứng giờ mua qua Shop theo từng instance sở hữu riêng.

### Android — Economy v2: Xu Lá + Giờ tích luỹ + ấp trứng theo bình (2026-07-23)

Cập nhật toàn bộ tầng data + UI của T-029→T-031 theo economy v2 phía backend (T-068). Đã verify từng nhánh thật trên emulator (`tester01@cozypomo.dev`), **chưa commit/push** — chờ xác nhận vì yêu cầu ban đầu chỉ nói rõ "release" cho backend, không nói rõ cho Android.

- **T-069 — Cập nhật Android theo economy v2.**
  - **Room (`data/local/session/SessionEntity.kt`):** bỏ `eggTypeId` bắt buộc, thay bằng `ownedEggId: String?`/`incubationRatio: Float?` + `minutesAccumulated`/`hatched`. DB version 1→2, dùng `fallbackToDestructiveMigration(true)` (app chưa release, không cần giữ dữ liệu cũ qua bump schema).
  - **Network DTO:** `OwnedEggDto` mới (map đúng `OwnedEgg` + `EggType`/`Species` lồng bên trong), `CreateSessionRequest`/`SessionDto`/`CompleteSessionResponse` đổi theo field mới của backend, `BalanceResponse` thêm `focusMinutes`, `GameSettingsDto` mới. `ApiService` thêm `getOwnedEggs(status)`/`getGameSettings()`.
  - **`TimerRepository`:** `startSession` nhận thêm `ownedEggId`/`incubationRatio`. `completeSession` giờ trả `SessionCompletionResult` dạng sealed interface 3 nhánh (`Hatched`/`Incubating`/`NoEgg`) qua `completionEvents: SharedFlow<...>` (đổi tên từ `hatchEvents` cũ) — UI phân biệt rõ 3 kết quả thay vì chỉ có "nở hay không".
  - **`HomeViewModel`/`HomeScreen`:** nút "+" mở `EggPickerDialog` viết lại hoàn toàn — hiện **trứng đang sở hữu** (`GET /owned-eggs?status=INCUBATING`) kèm thanh tiến trình `incubatedMin/hatchDurationMin` thật, có dòng "Không ấp trứng nào" luôn chọn được (đúng yêu cầu "không chọn cũng được"). Chọn trứng xong hiện thêm slider tỉ lệ ấp/tích luỹ (0–100%, mặc định 100% ấp). Khi đang chạy phiên có chọn trứng, hiện `IncubatingJar` — vầng hào quang mờ nhấp nháy quanh bình (animation "đang ấp trứng" theo đúng yêu cầu). 2 `BalancePill` hiện song song cả Xu Lá và Giờ tích luỹ, đọc qua `GET /currency/balance`.
  - **`SessionResultDialog`:** viết lại hoàn toàn thành modal có animation (scale+fade khi xuất hiện), 4 nhánh nội dung theo đúng yêu cầu "tuỳ theo case hiển thị nội dung chia buồn hoặc chúc mừng": `HatchedContent` (chúc mừng, pulse + màu theo cấp bậc loài, tái dùng từ bản trước), `IncubatingContent` (mới — thanh tiến trình ấp animate + "sắp nở rồi!"), `NoEggContent` (mới — hoàn thành phiên không ấp trứng), `GaveUpContent` (chia buồn, animation rung nhẹ, giữ nguyên từ bản trước). Tất cả đều hiện `ResultStatsRow` — số Xu Lá + Giờ tích luỹ nhận được, animate đếm số.
  - **Đã verify thật trên emulator** (không chỉ code review): chọn trứng → chạy phiên → fast-forward qua SQL (`UPDATE sessions SET plannedMin=1 WHERE status='RUNNING'`) → xác nhận modal "Incubating" hiện đúng số liệu thật (40/180 phút ấp, +25 Xu Lá, số dư 100→125) → mở lại egg picker xác nhận 5 trứng sở hữu hiện đúng tiến trình từng cái → xác nhận luồng Bỏ cuộc hiện đúng modal chia buồn ("Trứng đã vỡ rồi…"), số dư không đổi. **Chưa verify trực tiếp qua UI automation nhánh Hatched** (2 lần thao tác chạm nhầm trứng khác trong lúc test) — nhưng logic hatch phía backend đã verify qua curl riêng (`hatched:true`, roll ra loài thật), và `HatchedContent` tái dùng nguyên UI đã verify trực quan ở bản trước T-031.
  - **Khoảng trống đã biết:** Android **chưa có UI mua trứng thật** (Shop screen T-037 vẫn placeholder) — người dùng hiện chỉ chọn được trứng đã có sẵn từ seed data, chưa tự mua mới được từ trong app. Cũng chưa có nơi hiển thị `GameSettings.coinsPerFocusMinute` cho người dùng xem trước khi bắt đầu phiên (không bắt buộc theo yêu cầu, nhưng có thể cân nhắc thêm).
  - **[Đã cập nhật bởi T-072/T-076, xem bên dưới]** — copy Bỏ cuộc "Trứng đã vỡ rồi…" và Shop placeholder mô tả ở trên đã lỗi thời, giữ nguyên làm lịch sử.

### Android — Đợt cập nhật giao diện lớn: bình ấp/trứng, font, Khu rừng, Cửa hàng (2026-07-23)

Theo yêu cầu chi tiết của Dev1002 sau khi xem qua bản economy v2: đổi icon số dư có tooltip, trứng hiện trực tiếp trong bình (bỏ tên bên dưới) với hình ảnh đổi theo tiến trình ấp, sửa copy Bỏ cuộc (không còn "vỡ trứng"), gom cài đặt vào 1 màn riêng, Splash sinh động hơn + hiện version, đổi font toàn app, và xây thật T-035/T-036/T-037 bám sát wireframe gốc (kể cả hiệu ứng bồng bềnh nhẹ liên tục trên item Cửa hàng). Toàn bộ đã verify trực tiếp trên emulator (`tester01@cozypomo.dev`), **chưa commit/push**.

- **T-071 — Font Nunito toàn app.** Tải trực tiếp bản variable font (`res/font/nunito.ttf`, trục `wght`, đã xác nhận có subset `vietnamese` trong `METADATA.pb` của Google Fonts) — dùng `FontVariation.Settings` per-weight (400–800) thay vì nhiều file tĩnh. Viết lại toàn bộ `ui/theme/Type.kt` (trước đây chỉ set 4/15 slot `Typography`, còn lại là font hệ thống mặc định) để mọi text style trong app đều dùng Nunito. Verify trực quan: tiêu đề, số đếm ngược, nhãn tab, dấu tiếng Việt (ẫ, ộ, ữ...) hiện đúng, không vỡ font.
- **T-072 — Bình ấp/trứng: icon số dư + tooltip, trứng hiện trong bình theo tiến trình, sửa copy Bỏ cuộc.**
  - `ui/common/JarMark.kt` viết lại hoàn toàn: thêm tham số `eggColor`/`progress`/`desaturated`/`animate`. `jarEggStageFor(progress)` suy ra 4 giai đoạn từ tỉ lệ `incubatedMin/hatchDurationMin`: **NORMAL** (<50%, trứng trơn), **CRACKING** (50–85%, 2 vệt nứt), **ABOUT_TO_HATCH** (85–100%, 3 vệt nứt + rung lắc nhẹ liên tục quanh tâm trứng), **HATCHED** (100%, 2 mảnh vỏ tách đôi + quầng sáng). `HomeScreen.kt` tính `jarProgress` "sống" — cộng dồn ước tính phút đã trôi qua trong phiên đang chạy (theo `incubationRatio`) lên trên `incubatedMin` đã lưu, nên hình trứng đổi ngay khi đếm ngược chạy chứ không cần đợi hoàn thành phiên. Bỏ hẳn tên trứng hiển thị bên dưới bình — thay bằng dòng tiến trình "`{incubatedMin}/{hatchDurationMin} phút ấp`" (hoặc gợi ý "Chạm nút + để chọn trứng ấp" khi chưa chọn).
  - `BalancePill` (Home) đổi từ chấm màu tối giản sang icon thật (`Icons.Filled.Eco` cho Xu Lá, `Icons.Filled.HourglassBottom` cho Giờ tích luỹ) + chạm vào hiện `TooltipBox`/`PlainTooltip` giải thích chỉ số (dùng `rememberTooltipState(isPersistent = true)` vì cần bật bằng tap, không phải hover/long-press mặc định của Material3).
  - Sửa copy Bỏ cuộc — cả dialog xác nhận (`HomeScreen.kt`) lẫn modal kết quả (`SessionResultDialog.GaveUpContent`): bỏ hẳn khung "trứng sẽ vỡ", thay bằng "Bạn sẽ không nhận Xu Lá/Giờ tích luỹ cho phiên này, trứng đang ấp vẫn an toàn/giữ nguyên tiến trình cũ" — trứng vẫn vẽ màu xám (`EggIcon` alpha 0.45) để báo hiệu, không đổi hình dạng/vỡ. `SessionResultUi.GaveUp` nay mang theo `eggTypeName` để nhắc đúng tên trứng trong copy.
- **T-073 — Màn Cài đặt (S-07, phần lõi) — gom logout vào đây.** `ui/settings/SettingsScreen.kt` + `SettingsViewModel.kt` mới: hiện email tài khoản (`GET /auth/me`), số phiên bản (`BuildConfig.VERSION_NAME`), nút Đăng xuất (chuyển từ icon rời trên Trang chủ vào đây). Icon ⚙ trên `HomeScreen.kt` giờ điều hướng sang route `settings` mới trong `CozyPomoNavHost.kt` thay vì không làm gì như trước.
- **T-074 — Splash sinh động hơn + hiện version.** `SplashViewModel` đảm bảo hiện tối thiểu 2.2s (đo thời gian xử lý thật, `delay()` phần còn thiếu) — trước đây có thể chớp qua gần như ngay lập tức nếu đã có JWT sẵn. `SplashScreen.kt` thêm 3 icon loài (cáo/hoa/rùa, dùng chung bộ sinh hình T-075) xuất hiện so le kiểu bounce-in, và dòng "v{versionName}" mờ ở cuối màn hình.
- **T-075 — Port bộ sinh hình loài sang Compose Canvas + xây thật T-035/T-036.**
  - `ui/common/SpeciesArt.kt` — port 1:1 thuật toán từ `backend/src/admin/components/species-art.ts` (14 màu palette, 10 archetype Rừng + 10 Biển + 10 Thực vật + 5 Thần thú, hào quang theo cấp bậc B/A/S/SS/SSR, PRNG `mulberry32`/hash y hệt bản gốc để rotation "jitter" ổn định theo tên loài) sang `DrawScope` — dùng chung cho Splash, Khu rừng, Chi tiết loài, và icon trứng ở Cửa hàng. Có animation bồng bềnh liên tục (dịch translateY hình sin) + hào quang pulsing/xoay cho SSR.
  - **T-035 — Khu rừng/Bộ sưu tập (S-04) thật.** `ui/forest/ForestViewModel.kt` + `ForestScreen.kt`: tải toàn bộ `GET /species` + `GET /collection` + `GET /collection/progress` 1 lần, lọc theo tab (Tất cả/Thú rừng/Sinh vật biển/Thực vật) ở client. Lưới 3 cột: thẻ đã mở khoá hiện `SpeciesArtIcon` thật + badge cấp bậc + số lần nở; thẻ khoá hiện bóng tròn xám + "?". Tab dạng `FilterChip` trong `Row` cuộn ngang (`horizontalScroll`) — tránh tràn màn hình khi 4 tab không đủ chỗ.
  - **T-036 — Chi tiết loài/Lore (S-03) thật.** `ui/forest/SpeciesDetailDialog.kt` — popup (không phải route riêng, đúng tinh thần tech-spec "Popup khi chạm thẻ"): ảnh loài lớn, badge cấp bậc, số lần nở, lore text (có câu mặc định nếu `lore` null), nút yêu thích gọi `PATCH /collection/:speciesId/favorite`.
  - **Bug gặp & fix:** response thật của `PATCH .../favorite` **không** kèm `species` lồng bên trong (khác `GET /collection` có `include: { species: true }`) — decode thẳng thành `CollectionEntryDto` (field `species` bắt buộc) làm kotlinx-serialization ném lỗi thiếu field, thất bại **âm thầm** trong `runCatching` nên tim yêu thích không bao giờ đổi dù server trả `200`. Fix: thêm `FavoriteToggleResponseDto` khớp đúng response thật, merge kết quả vào entry cũ trong `ForestViewModel` thay vì thay nguyên object. Đã verify lại: tim đổi trạng thái đúng cả 2 chiều.
- **T-076 — Màn Cửa hàng (S-05) thật, mua trứng bằng Xu Lá hoặc Giờ tích luỹ.** `ui/shop/ShopViewModel.kt` + `ShopScreen.kt`: 3 tab (Trứng mới/Bình thuỷ tinh/Nhạc nền, `GET /shop-items?category=`), mỗi item có icon (trứng dùng `EggIcon` theo `colorHex`; bình/nhạc dùng icon Material) bọc trong `FloatingIcon` (bồng bềnh liên tục, đúng hiệu ứng "infinity" ở wireframe). Nút mua có 3 trạng thái: **Đã sở hữu** (bình/nhạc mua rồi, theo `GET /inventory`), **Cần thêm Xu** (không đủ Xu Lá, chỉ áp dụng bình/nhạc), **Mua ngay**. Vật phẩm EGG khi bấm mua mở dialog chọn trả bằng Xu Lá hay Giờ tích luỹ (mỗi lựa chọn tự disable nếu số dư không đủ) trước khi gọi `POST /shop-items/:id/purchase` với `payWith` tương ứng. Endpoint mua trả về `ResponseBody` thô (không parse) vì response thật khác kiểu tuỳ danh mục (`OwnedEgg` cho EGG, `InventoryItem` cho còn lại) — không có 1 DTO chung an toàn để decode, chỉ cần biết thành công rồi gọi lại `getBalance`/`getOwnedEggs`/`getInventory`.
  - **Bug gặp & fix:** header Row ban đầu để tiêu đề dài "Tiệm Tạp Hóa Rừng Xanh" cạnh tranh chỗ với 2 `BalancePill` trong cùng 1 `Row` không `weight` — pill Giờ tích luỹ bị bóp gần bằng 0 khiến text "20p" vỡ dòng ký tự-một ("2/0/p"). Fix: `Modifier.weight(1f)` + `maxLines=1`/`Ellipsis` cho tiêu đề, để 2 pill luôn giữ kích thước tự nhiên.
  - **Đã verify thật trên emulator:** mua "Trứng Hoa" (30 Xu) — dialog chọn tiền tệ hiện đúng, phương án "30 phút Giờ tích luỹ" tự disable vì số dư khi đó chỉ có 20p, chọn "30 Xu Lá" → mua thành công, Snackbar "Đã mua Trứng Hoa!", số dư giảm đúng `125→95`.
  - **Khoảng trống còn lại:** chưa có màn Kho đồ (Inventory) để xem/trang bị (`equip`) bình/nhạc đã mua — `PATCH /inventory/:id/equip` đã có ở backend + `ApiService`, chỉ chưa có UI gọi tới (không nằm trong 3 tab wireframe gốc, để lại cho T-039 đầy đủ).

### Backend + Android — Economy v3: chọn 1 loại thưởng mỗi phiên, tỉ giá cố định 1:10 (2026-07-23)

Theo phản hồi của Dev1002 sau khi kiểm thử economy v2: trước đây mỗi phiên hoàn thành luôn cộng **cả** Xu Lá lẫn Giờ tích luỹ (phần không dành để ấp trứng) — yêu cầu sửa lại chỉ được nhận **một trong hai**, người dùng tự chọn, và ấn định tỉ giá quy đổi rõ ràng (1 phút Giờ tích luỹ = 10 Xu Lá) áp dụng luôn cho giá Cửa hàng. Đã verify qua `curl` (backend) và emulator thật, **chưa commit/push**.

- **T-077 — Session mang thêm `rewardCurrency`, chỉ cộng 1 loại tiền tệ khi hoàn thành.**
  - Backend: `schema.prisma` — `Session.rewardCurrency: CurrencyType @default(COIN)`, `GameSettings.coinsPerFocusMinute` mặc định `1 → 10`. Migration `20260723161222_reward_currency_choice`.
  - `sessions.service.ts.complete()` viết lại: phần phút *không* dành ấp trứng (`remainingMin = plannedMin - minutesIncubated`) quy đổi **chỉ** ra `rewardCurrency` đã chọn lúc bắt đầu phiên — Giờ tích luỹ thì `minutesAccumulated = remainingMin, coinsEarned = 0`, Xu Lá thì ngược lại theo `coinsPerFocusMinute`. Chỉ tạo **1** dòng ledger mỗi phiên (trước đây tạo 2 — 1 cho mỗi loại tiền).
  - `create-session.dto.ts`/`sync.service.ts` nhận thêm field `rewardCurrency`.
  - Seed: `coinsPerFocusMinute: 10`; giá trứng Cửa hàng nhân theo tỉ giá cố định `priceCoin = priceHours * 10` (Hoa 30→300, Biển 60→600, Rừng 90→900, Bí Ẩn 150→1500); vòng lặp mô phỏng phiên cho tester cũng sửa theo đúng logic 1-tiền-tệ-mỗi-phiên ở trên (trước đó seed cũ vẫn cộng cả 2 loại, không khớp code service mới).
  - Android: `TimerRepository.startSession()` nhận thêm `rewardCurrency`, lưu vào `SessionEntity` (DB version 2→3, vẫn `fallbackToDestructiveMigration`), fallback offline trong `completeSession()` tôn trọng đúng lựa chọn thay vì luôn cộng cả 2. `HomeViewModel`/`HomeScreen` thêm chip "Nhận thưởng bằng: Xu Lá / Giờ tích luỹ" (khoá khi đang chạy phiên). `SessionResultDialog.ResultStatsRow` chỉ hiện dòng tiền tệ nào `> 0` (trước đây luôn hiện cả 2 dòng, dòng còn lại luôn là 0 gây rối mắt).
  - **Đã verify:** `curl` xác nhận tạo phiên với `rewardCurrency=FOCUS_MINUTE` → hoàn thành → chỉ `minutesAccumulated` tăng, `coinsEarned=0`; reseed lại xác nhận giá Cửa hàng đúng tỉ giá mới.

- **T-078 — Seed thêm loài đa dạng cho tester (Thú rừng/Sinh vật biển/Thực vật/Thần thú).**
  - Trước đó seed chỉ đảm bảo mỗi tester có 2 loài MYTHIC, các nhóm còn lại phụ thuộc hoàn toàn vào roll ngẫu nhiên nên nhiều tester (kể cả `tester01`) không có đại diện nhóm FOREST/SEA/PLANT nào để kiểm thử Khu rừng. Thêm khối `bonusPools` đảm bảo tối thiểu: 3 FOREST + 2 SEA + 3 PLANT + 2 MYTHIC mỗi tester, ngẫu nhiên chọn loài trong mỗi nhóm + `hatchCount`/`isFavorite` ngẫu nhiên.
  - **Đã verify:** reseed xong, `GET /collection` của `tester01` trả về đủ cả 4 nhóm (`{FOREST:3, SEA:2, PLANT:3, MYTHIC:2}` + các loài roll thêm từ mô phỏng phiên) — xác nhận trực quan "Cáo Pomodoro" (FOREST) hiện trong lưới Khu rừng, trước đó nhóm này trống.

- **T-079 — Sửa animation trứng trong bình: chuyển động liên tục ở mọi giai đoạn.** `JarMark.kt` trước đây chỉ rung nhẹ khi vào giai đoạn `ABOUT_TO_HATCH` (≥85% tiến trình) — các giai đoạn còn lại trứng đứng yên hoàn toàn, không có cảm giác "đang ấp". Sửa: biên độ lắc (`wobbleAmplitude`) giờ luôn > 0 khi `animate=true`, tăng dần theo tiến trình (`2 + progress * 10`), dùng dao động liên tục 2 chiều (`wobblePhase * 2 - 1`) thay vì chỉ lắc khi đạt ngưỡng.

- **T-080 — Sửa Splash: version bị ẩn sau thanh điều hướng + thêm âm thanh chào mừng.**
  - Nguyên nhân version không hiện: `MainActivity` gọi `enableEdgeToEdge()` nên nội dung vẽ tràn ra sau thanh điều hướng hệ thống (kể cả thanh 3 nút opaque) — dòng version chỉ có `padding(bottom=24dp)` bị vẽ **dưới** thanh điều hướng, không phải lỗi logic hiển thị. Fix: thêm `.safeDrawingPadding()` vào `Box` gốc của `SplashScreen`.
  - Thêm âm thanh chào mừng: `res/raw/welcome_chime.wav` (4 nốt đi lên C5→E5→G5→C6, ~0.78s, tự tổng hợp bằng `ffmpeg`, không dùng nhạc có bản quyền), phát qua `MediaPlayer` trong `LaunchedEffect(Unit)`, tự `release()` khi phát xong.

- **T-081 — Hiển thị tài khoản đang đăng nhập ổn định, không phụ thuộc gọi `/auth/me` (dễ 401 vì JWT chỉ sống 15 phút).** `AuthPreferences`/`AuthRepository` lưu thêm `accountEmail`/`accountId` vào DataStore ngay lúc đăng nhập thành công (`persistSession()`), đọc lại qua 2 `Flow` mới thay vì phải gọi API mỗi lần vào Cài đặt. Đây là giải pháp tạm cho tới khi có refresh-token flow thật (xem mục Chưa làm).

### Android — Bubble số dư dùng chung, Kho Trứng, menu cheat cho tester (2026-07-23)

Theo phản hồi kèm ảnh chụp màn hình cho thấy 2 pill số dư bị kẹt "…" ở các tab ngoài Trang chủ. Yêu cầu: đảm bảo số dư luôn hiện đầy đủ ở mọi màn, thêm mục Kho Trứng vào Khu rừng, bổ sung ID tài khoản + số dư vào Cài đặt, và xây bubble menu cheat chỉ dành cho tài khoản tester để tự kiểm thử phần thưởng/vật phẩm mà không cần chờ thật. Đã verify trên emulator, **chưa commit/push**.

- **T-082 — `CurrencyViewModel` dùng chung 1 instance cho cả 4 tab + Cài đặt, bubble số dư không bao giờ kẹt "…".**
  - Nguyên nhân lỗi cũ: mỗi màn (Trang chủ, Cửa hàng...) tự gọi `GET /currency/balance` riêng và tự quản lý state riêng — màn nào chưa load xong hoặc gọi lỗi thì hiện "…" mãi, kể cả khi màn khác đã có số liệu đúng.
  - Fix: `ui/common/CurrencyViewModel.kt` mới (Hilt), tạo **1 lần duy nhất** trong `CozyPomoNavHost` (scope theo `NavBackStackEntry` "main" của `RootNavHost`, sống suốt khi chuyển tab) rồi truyền xuống từng màn con thay vì mỗi màn tự `hiltViewModel()` mặc định. Khi gọi lại balance thất bại, **giữ nguyên giá trị cũ** thay vì reset về `null` — đây là phần fix quan trọng nhất, tránh việc 1 lần gọi mạng lỗi làm mất hẳn số liệu đã có.
  - `ui/common/BalanceBubble.kt` mới — gom `BalancePill`/`CurrencyBubble` dùng chung (trước đây `HomeScreen`/`ShopScreen` mỗi nơi tự có bản riêng bị trùng lặp).
  - **2 bug layout gặp khi lắp bubble vào `topBar` của `Scaffold`:** (1) Ban đầu đặt bubble như 1 `Box` nổi tự do đè lên `NavHost` — khi bỏ pill cũ khỏi `HomeScreen` (do chuyển sang bubble dùng chung), layout Trang chủ đẩy nút "+" chọn trứng lên cao hơn, va chạm với bubble nổi. Fix: chuyển bubble vào slot `topBar` thật của `Scaffold` để Scaffold tự tính `innerPadding` đúng cho mọi tab, không thể va chạm nội dung riêng của từng màn dù màn đó bố cục thế nào. (2) Sau khi chuyển vào `topBar`, bubble đè lên icon thanh trạng thái (wifi/pin) vì `Box` topBar tự viết không tự động có inset thanh trạng thái như `TopAppBar` — fix bằng cách thêm `.statusBarsPadding()` thủ công.
  - `HomeScreen`/`ShopScreen` bỏ hẳn state balance riêng (`loadBalance()`/`loadInventoryAndBalance()` cũ), nhận `currencyViewModel` làm tham số, tự `refresh()` sau khi hoàn thành phiên/mua hàng.
  - **Đã verify:** chụp ảnh trước/sau xác nhận bubble hiện đúng "2140"/"98p" ở mọi tab (Khu rừng, Cửa hàng, Cài đặt), không còn "…", không đè lên nội dung màn hình hay thanh trạng thái.

- **T-083 — Cài đặt: hiện ID tài khoản (chạm để copy) + số dư Xu Lá/Giờ tích luỹ.**
  - `SettingsScreen.kt` thêm dòng ID (rút gọn hiện `ID: {8 ký tự đầu}…` để tránh vỡ dòng, nhưng copy **toàn bộ** UUID thật khi chạm, qua `LocalClipboardManager` + `Toast` xác nhận) và mục "Số dư" hiện 2 `BalancePill` dùng chung từ `currencyViewModel`.
  - **Bug gặp & fix:** hiện nguyên UUID đầy đủ ban đầu vỡ thành 2 dòng xấu — rút gọn còn 8 ký tự + "…" khi hiển thị (copy vẫn lấy full ID).
  - **Bug gặp & fix:** `Column` dùng `Spacer(Modifier.weight(1f))` để đẩy nút Đăng xuất xuống đáy — sau khi thêm mục "Số dư" + dòng ID 2 dòng, tổng chiều cao nội dung vượt màn hình, nút Đăng xuất bị cắt mất (co lại thành 1 vệt không đọc được) do `weight()` không tương thích với `verticalScroll` trên cùng `Column` (giới hạn cứng của Compose, không phải lỗi style). Fix: thêm `.verticalScroll(rememberScrollState())` cho `Column` + thay `Spacer(weight(1f))` bằng `Spacer(height(32.dp))` cố định.

- **T-084 — Kho Trứng trong Khu rừng: xem trứng đang ấp + tiến trình dạng bubble khi chạm.**
  - Thêm `SpeciesCategoryFilter.EGG_STORAGE` làm tab thứ 5 (chip cuối, sentinel `backendValue` không gửi lên API filter loài). `ForestViewModel.load()` gọi thêm `GET /owned-eggs?status=INCUBATING`. Khi chọn tab này, `ForestScreen` hiện lưới `EggStorageCard` thay cho lưới loài — mỗi thẻ hiện icon trứng + tên loại + tiến trình `{đã ấp}/{tổng phút}` ngay trên thẻ, chạm vào hiện thêm bubble tooltip (`TooltipBox`/`PlainTooltip`, tap-to-show) nhắc lại "`{đã ấp}/{tổng phút} phút đã ấp`".
  - **Bug gặp & fix:** dùng tên đủ (fully-qualified) `androidx.compose.material3.PlainTooltip { ... }` ngay trong code thay vì `import` chuẩn — báo lỗi biên dịch "Unresolved reference" kéo theo hàng loạt lỗi "chỉ gọi được từ hàm @Composable", dù cùng 1 pattern hoạt động bình thường ở `BalanceBubble.kt` khi dùng qua `import`. Fix: thêm `import` chuẩn, dùng tên ngắn — xác nhận trình biên dịch Compose không đáng tin cậy với lệnh gọi FQN ở vị trí trailing lambda.
  - **Đã verify trên emulator (`tester01`):** cuộn ngang hàng chip lộ đúng "Kho Trứng" (lưu ý: toạ độ chạm/vuốt lấy từ ảnh chụp bị co tỉ lệ hiển thị — phải lấy toạ độ thật qua `adb shell uiautomator dump` mới vuốt đúng), chạm vào hiện đúng lưới 5 trứng sở hữu (2 Trứng Bí Ẩn, Trứng Hoa, Trứng Biển, Trứng Rừng) kèm tiến trình đúng số liệu thật, chạm 1 thẻ hiện đúng bubble "75/120 phút đã ấp".

- **T-085 — Bubble menu cheat cho tester (debug/QA), chạm 5 lần vào "Phiên bản" để bật/tắt.**
  - Backend (`backend/src/debug/`): `DebugModule`/`DebugController`/`DebugService` — 3 endpoint `POST /debug/grant-currency|grant-egg|grant-species`, tất cả `@UseGuards(JwtAuthGuard)` **và** kiểm tra thêm `TESTER_EMAIL_PATTERN = /^tester\d{2}@cozypomo\.dev$/` khớp với email thật trong JWT trước khi cho thực hiện — chặn ở **server**, không chỉ ẩn nút ở UI, nên gọi thẳng API cũng không cheat được nếu không phải tài khoản tester.
  - Android (`SettingsViewModel`/`SettingsScreen`): đếm số lần chạm "Phiên bản" (chỉ hoạt động nếu `isTester`), đủ 5 lần thì bật/tắt `Dialog` `TesterCheatMenu` liệt kê 9 nút: +1000 Xu Lá, +1000 Giờ tích luỹ, kéo phiên đang chạy về còn ~5s (chỉnh thẳng `startElapsedRealtimeMs` trong Room qua `TimerRepository.debugFastForwardActiveSession()`, tận dụng việc `TimerForegroundService` luôn đọc lại session mới nhất từ Room mỗi giây), nhận Trứng Bí Ẩn, và nhận ngẫu nhiên loài cấp B/A/S/SS/SSR. Kết quả mỗi lệnh hiện qua `Snackbar` (không tự tắt, phải bấm "Đóng" — chấp nhận được vì đây là menu debug nội bộ, không phải luồng chính).
  - **Đã verify:** `curl` xác nhận cả 3 endpoint (grant-currency COIN +1000 → số dư đúng, grant-egg mặc định → tạo đúng Trứng Bí Ẩn, grant-species SSR → roll đúng 1 loài SSR thật); emulator xác nhận nút "+1000 Xu Lá" và "Nhận ngẫu nhiên hạng B" hoạt động đúng qua UI thật, số dư/collection cập nhật ngay.

### Android — Bubble cheat kiểu "chat head" Messenger, làm đẹp Cài đặt, sửa lưới Khu rừng (2026-07-23)

Theo phản hồi tiếp theo của Dev1002 sau khi dùng thử T-085: bubble cheat trước đó chỉ là 1 `Dialog` bật/tắt ngay tại Cài đặt, không thực sự "nổi" trên mọi màn như Messenger; trang Cài đặt hiện ID dạng rút gọn `...` thay vì đầy đủ và số dư dùng lại pill nhỏ giống Trang chủ (không "tinh tế" như yêu cầu); lưới Khu rừng bị lệch hàng khi có thẻ đã mở khoá, mất hiệu ứng hào quang, và badge cấp bậc chưa gọn. Đã verify trên emulator, **chưa commit/push**.

- **T-086 — Bubble cheat nổi kéo-thả được, hiện trên MỌI tab; tách logic ra khỏi Cài đặt.**
  - `ui/common/TesterCheatViewModel.kt` mới (chuyển toàn bộ state/logic cheat từ `SettingsViewModel` sang đây) — tạo 1 lần duy nhất ở `CozyPomoNavHost` (cùng tầng với `currencyViewModel`) nên sống độc lập với Settings, không bị huỷ khi rời màn Cài đặt. Thêm state `bubbleVisible`/`showDialog` tách biệt: 5 lần chạm "Phiên bản" ở Cài đặt chỉ bật/tắt `bubbleVisible`; chạm vào bubble mới mở `showDialog`.
  - `ui/common/CheatBubble.kt` mới — bubble tròn 56dp kéo-thả tự do trong `BoxWithConstraints` bọc toàn bộ `CozyPomoNavHost` (render ngoài `Scaffold`, đè lên mọi tab kể cả bottom nav). Dùng 2 `Modifier.pointerInput` riêng biệt: `detectTapGestures` (mở menu) + `detectDragGestures` (kéo, có `change.consume()` + `coerceIn` giữ trong màn hình).
  - **Bug gặp & fix:** lần đầu dùng 1 `detectDragGestures` duy nhất, tự đếm quãng đường kéo trong `onDragStart`/`onDragEnd` để suy ra "đây là tap hay drag" — nhưng `detectDragGestures` của Compose **không bao giờ gọi** `onDragStart`/`onDragEnd` cho 1 lần chạm đứng yên (chưa vượt touch slop), nên tap thật sự không bao giờ mở được menu. Fix theo đúng khuyến nghị Compose: 2 `pointerInput` độc lập, tap dùng `detectTapGestures` thật.
  - `ui/common/TesterCheatMenu.kt` mới (nội dung y hệt Dialog cũ, chỉ chuyển vị trí file) — render ở `CozyPomoNavHost` cùng với `Snackbar` kết quả, không còn nằm trong `SettingsScreen.kt`.
  - **Đã verify trên emulator:** bubble hiện đúng ở Trang chủ/Khu rừng/Cửa hàng/Cài đặt (test cả khi đang xem thẻ SSR ở Khu rừng), chạm mở đúng menu, kéo đổi vị trí được, +1000 Xu Lá test qua UI thật cập nhật bubble số dư ngay (`2140→3140`).

- **T-087 — Cài đặt: ID đầy đủ ở cuối trang (chạm để copy), số dư trình bày "tinh tế" hơn kiểu pill Trang chủ.**
  - `SettingsScreen.kt`: bỏ dòng ID rút gọn khỏi mục "Tài khoản" — chuyển xuống **cuối trang** (sau nút Đăng xuất), hiện **toàn bộ** UUID (không rút gọn `...`), cỡ chữ nhỏ (`labelSmall`), căn giữa, chạm vẫn copy full ID qua `LocalClipboardManager` như cũ.
  - Mục "Số dư" đổi từ 2 `BalancePill` (pill tròn nhỏ, giống hệt Trang chủ) sang `SettingsBalanceRow` mới — mỗi dòng: icon trong nền tròn nhạt màu + tên đầy đủ ("Xu Lá", "Giờ tích luỹ") + số liệu lớn căn phải, ngăn cách bằng `HorizontalDivider` mảnh — rõ ràng và khác hẳn kiểu pill gọn, đúng tinh thần "không cần giống ngoài trang home".
  - **Bug gặp lúc test, KHÔNG phải bug thật (ghi lại vì tốn nhiều công điều tra):** sau khi build, không thấy dòng ID đâu cả kể cả khi thêm `Log.d` xác nhận `uiState.userId` đúng giá trị và thêm hẳn 1 `Box` màu đỏ 80dp để test — hoá ra do `Column` dùng `.verticalScroll()` và tổng nội dung (đã dài hơn do thêm mục "Số dư") **cao hơn viewport thật sự** dù nhìn ảnh chụp tưởng còn nhiều khoảng trống phía dưới Đăng xuất; phải vuốt lên mới thấy. Không phải lỗi State/DataStore/Compose — chỉ là chưa cuộn xuống khi tự kiểm tra qua `uiautomator dump` (các node ở dưới viewport bị lược khỏi cây accessibility, gây hiểu lầm "không render").

- **T-088 — Khu rừng: sửa lưới lệch hàng, phục hồi hào quang, xếp badge gọn hơn.**
  - `ForestScreen.kt`: `UnlockedSpeciesCard`/`LockedSpeciesCard`/`EggStorageCard` giờ dùng chung 1 chiều cao cố định `SpeciesCardHeight = 134.dp` — trước đó thẻ đã mở khoá (có badge + icon + tên + số lần nở) cao hơn hẳn thẻ khoá (chỉ 1 vòng tròn "?"), khiến `LazyVerticalGrid` xếp lệch hàng theo chiều dọc.
  - Thêm nền tròn tương phản `surfaceContainerHighest` (56dp) phía sau `SpeciesArtIcon` (42dp) trong mỗi thẻ — nguyên nhân hào quang "biến mất" không phải do thiếu code (đã có từ T-075, thuật toán giống hệt backend) mà do màu hào quang cấp B/A (alpha rất thấp, 0.16–0.2) gần trùng tông màu nền thẻ (`surfaceContainer`) nên gần như vô hình; đặt trên nền tương phản hơn giúp hào quang hiện rõ (xác nhận trực quan qua loài cấp SS/SSR — vòng hào quang vàng/cam nổi bật hẳn).
  - `RarityBadge` chuyển từ xếp hàng trên cùng Column (chiếm chỗ, góp phần gây lệch hàng) sang overlay góc trên-trái thẻ (`Box` + `Modifier.align(Alignment.TopStart)` + `.shadow(2.dp, RoundedCornerShape(50))`) — không chiếm layout, nhìn "tinh tế" hơn kiểu ruy băng góc thẻ.
  - Tăng nhẹ biên độ bồng bềnh của `SpeciesArtIcon` (từ `-3f..+3f` lên `-4.5f..+4.5f` px) — icon loài vẽ chi tiết hơn icon Cửa hàng nên cần rõ hơn mới cảm nhận được ở size 42dp.
  - **Đã verify trên emulator:** lưới 3 cột giờ thẳng hàng hoàn toàn bất kể ô nào đã mở khoá; thẻ SSR ("Phượng Hoàng Bình Minh") và SS ("Gấu Mèo Sương Sớm") hiện hào quang rõ rệt nhiều vòng; badge "B"/"SS" hiện gọn ở góc thẻ, không đẩy icon xuống nữa.

- **T-089 — Sửa 2 bug Dev1002 báo trực tiếp: trứng nở không hiện hình loài, Khu rừng không cập nhật ngay sau khi cheat cấp loài/trứng.**
  - **Bug 1 — Modal "Trứng đã nở" chỉ hiện 1 chấm tròn màu phẳng, không phải hình loài thật.** Nguyên nhân: `SessionCompletionResult.Hatched` (client) chỉ giữ `speciesName`/`speciesRarity` từ response BE, **bỏ qua** `category`/`archetype`/`paletteIdx` mà `resultSpecies` trả về sẵn — `SessionResultDialog.HatchedContent` vì vậy chỉ vẽ được 2 `Box` tròn lồng nhau thay vì gọi `SpeciesArtIcon` (vốn cần đủ 3 trường này để chọn đúng bộ vẽ). Fix: thêm 3 trường vào `Hatched`, truyền từ `TimerRepository.completeSession()`, `HatchedContent` gọi `SpeciesArtIcon` thật (giữ fallback vòng tròn phẳng nếu thiếu dữ liệu — phòng trường hợp phiên cũ trước bản fix).
  - **Bug 2 — Cấp loài/trứng qua bubble cheat không hiện ngay trong "Khu rừng của tôi", phải rời tab rồi quay lại mới thấy.** Nguyên nhân: `ForestViewModel` chỉ tải dữ liệu 1 lần lúc `init`, không có cách nào biết dữ liệu vừa đổi từ nơi khác (bubble cheat sống ở tầng NavHost, không cùng phạm vi ViewModel với Forest). Fix: thêm `data/events/CollectionEventBus.kt` — 1 `@Singleton` bọc `MutableSharedFlow<Unit>` dùng chung qua Hilt; `TesterCheatViewModel` gọi `notifyChanged()` sau khi `cheatGrantSpecies`/`cheatGrantMysteryEgg` thành công; `ForestViewModel` lắng nghe `collectionEventBus.changes` và tự `load()` lại — không cần 2 ViewModel biết trực tiếp về nhau.
  - **Đã verify trên emulator:** đứng nguyên ở tab Khu rừng/Kho Trứng, mở bubble cheat cấp "Trứng Bí Ẩn" và cấp ngẫu nhiên hạng SSR — cả hai xuất hiện ngay lập tức trong Kho Trứng/lưới loài mà không cần rời tab (số loài mở khoá tăng `16→17`, thẻ "Phượng Hoàng Bình Minh" hiện hào quang SSR ngay). Xác nhận thêm qua thực nghiệm: 1 phiên ấp Trứng Biển (75→120 phút) hoàn thành thật (không qua cheat fast-forward giả lập số liệu) khiến trứng biến mất khỏi Kho Trứng và số loài mở khoá tăng thêm — xác nhận logic nở trứng phía sau vẫn đúng, chỉ riêng phần hiển thị hình ảnh trong modal là cần fix.
  - **Hạn chế khi verify:** không chụp được ảnh màn hình modal "Hatched" với hình loài thật trong phiên test này do JWT hết hạn 15 phút + trục trặc thao tác UI-automation (chọn nhầm trứng, cuộn danh sách egg-picker) làm gián đoạn nhiều lần giữa lúc phiên hoàn thành và lúc chụp — nhưng đã xác nhận chắc chắn qua log + số liệu rằng nở trứng vẫn hoạt động đúng, và code fix chỉ đơn thuần truyền tiếp 3 trường dữ liệu BE đã trả sẵn vào đúng composable `SpeciesArtIcon` đã verify hoạt động tốt ở Khu rừng — rủi ro sai sót thấp.

### ✅ Release backend-v1.20260723.004 + app-v1.20260723.002, reseed production (2026-07-23)

- **T-090 — Push toàn bộ batch T-077→T-089 lên `main`, release cả backend lẫn Android, reseed production.**
  - Commit theo 4 phần rõ ràng (backend economy v3 + debug module; toàn bộ batch UI Android; cập nhật `plan.md`; bump version Android) rồi `git push` fast-forward thẳng lên `origin/main` (không qua PR, đúng thói quen của repo — lịch sử toàn commit thẳng, không có merge commit).
  - **Backend:** tạo GitHub Release `backend-v1.20260723.004` → `backend-deploy.yml` tự chạy build image + push `ghcr.io` + SSH deploy (`docker compose pull && up -d`) — xác nhận thành công qua `gh run watch` (cả 2 job `build-and-push` và `deploy` đều xanh) và `curl /health` trả `200` đúng ngay sau đó.
  - **Bug phát hiện khi verify sau deploy:** `GET /game-settings` trên production vẫn trả `coinsPerFocusMinute: 1` dù code + migration đã lên `main` — nguyên nhân: migration chỉ có `ALTER COLUMN ... SET DEFAULT 10`, đổi **default cho dòng mới**, không đụng tới dòng `GameSettings` **đã tồn tại sẵn** từ lần seed trước. Tỉ giá 1:10 vì vậy không thực sự có hiệu lực trên production dù đã "release xong".
  - Hỏi Dev1002 cách xử lý — chọn phương án **reseed lại toàn bộ production** (như lần economy v2 trước) thay vì chỉ UPDATE 1 dòng. Kích hoạt job `reseed-production` có sẵn (gated kép: `reseed=true` + gõ đúng `RESEED`) qua `gh workflow run backend-deploy.yml --ref main -f reseed=true -f confirm_reseed=RESEED` — chạy thành công, xác nhận lại `GET /game-settings` trả đúng `coinsPerFocusMinute: 10`, đăng nhập `tester01@cozypomo.dev` bằng mật khẩu chuẩn vẫn hoạt động (tài khoản tester được tạo lại từ seed mới).
  - **Android:** bump `versionCode 1→2`, `versionName "0.1.0"→"0.2.0"` (release đầu tiên chưa từng bump version dù đã tồn tại `app-v1.20260723.001` — lần này nội dung tăng đáng kể nên bump cho đúng). Dev1002 xác nhận đã tự cấu hình xong 4 GitHub Secrets ký release (`ANDROID_KEYSTORE_BASE64/PASSWORD`, `ANDROID_KEY_ALIAS/PASSWORD`) — tạo GitHub Release `app-v1.20260723.002` → `android-release.yml` chạy **lần đầu tiên với secrets thật** (trước đó T-054 ghi nhận "chưa test lần nào"), build `bundleRelease`/`assembleRelease` ký bằng keystore thật, upload `app-release.aab` + `app-release.apk` vào release — xác nhận qua `gh run watch` (job `build` xanh) và `gh release view` thấy đủ 2 asset.
  - **Kết quả:** cả backend lẫn Android đều đã có bản release thật đang chạy khớp nhau (tỉ giá 1:10, chọn 1 loại thưởng, Khu rừng/Cửa hàng/Cài đặt/bubble cheat...); T-053/T-054 (Nhóm D) coi như xong.

### Android — Bug tiến trình ấp trứng, redesign bubble cheat, refresh-token, màn Thống kê + Cài đặt còn lại (2026-07-24)

Theo yêu cầu tiếp theo của Dev1002: sửa bug hiển thị tiến trình ấp trứng, làm đẹp lại modal bubble cheat, xây refresh-token flow (điểm nghẽn ưu tiên cao nhất còn lại), và xây 2 màn còn thiếu trong 4 tab chính + Cài đặt (T-038/T-039), **chủ động bỏ hẳn Sao lưu Google Drive (S-07a/`SyncRepository`)** khỏi phạm vi theo yêu cầu — không phải hoãn lại, mà không làm. Đã build `compileDebugKotlin` thành công (toàn bộ module), **chưa chạy tay trên emulator, chưa commit/push**.

- **T-091 — Fix bug: tiến trình ấp trứng không cập nhật ngay khi phiên hoàn thành.**
  - Nguyên nhân: `HomeViewModel` chỉ gọi `loadOwnedEggs()` (bất đồng bộ, phải đợi mạng) khi nhận `completionEvents` — trong lúc đó, `sessionState` (đọc từ Room, đổi gần như ngay lập tức khi `TimerRepository.completeSession()` ghi `status=COMPLETED`) đã chuyển Running→Idle, khiến `HomeScreen.jarProgress` rơi vào nhánh `else` dùng `selectedEgg.incubatedMin` **CŨ** (giá trị trước khi phiên vừa hoàn thành) cho tới khi network kịp trả về — tạo cảm giác "chưa cập nhật ngay".
  - Fix: `HomeViewModel` vá `selectedOwnedEgg` NGAY (đồng bộ, trong cùng lần cập nhật state với `sessionResult`) từ chính dữ liệu đã có sẵn trong `SessionCompletionResult` (`Incubating.incubatedMin`, hoặc `null` nếu `Hatched` vì trứng không còn ở trạng thái INCUBATING nữa) — không cần đợi `loadOwnedEggs()` mới thấy đúng số liệu.
  - Bug thứ 2 cùng gốc: `TimerRepository.completeSession()` trước đây không báo cho `CollectionEventBus` — nếu đang đứng ở tab Khu rừng/Kho Trứng khi 1 phiên hoàn thành ở Trang chủ (hoặc Foreground Service tự hoàn thành lúc không mở app), tiến trình ấp ở đó vẫn hiện số liệu cũ tới khi rời rồi quay lại tab, y hệt lỗi cheat-bubble đã sửa ở T-089 nhưng cho trường hợp hoàn thành phiên thật. Fix: `TimerRepository` inject thêm `CollectionEventBus`, gọi `notifyChanged()` sau khi emit kết quả nếu có `ownedEgg`/`hatched` liên quan.
- **T-092 — Redesign bubble cheat (Tester) thành lưới thẻ nhỏ.** `TesterCheatMenu.kt` viết lại hoàn toàn: bỏ `LazyColumn` nút full-width cũ, thay bằng 1 hàng 3 thẻ vuông (Xu Lá/Giờ tích luỹ/Kéo phiên) + lưới 3 cột (Trứng Bí Ẩn + 5 thẻ hạng B/A/S/SS/SSR, tái dùng màu `RARITY_BADGE` có sẵn từ `SpeciesArt.kt` cho đúng ngôn ngữ hình ảnh với Khu rừng) — gọn và "tinh tế" hơn theo đúng yêu cầu.
- **T-093 — Refresh-token flow cho `AuthRepository`.** Điểm nghẽn ưu tiên cao nhất còn lại theo ghi nhận ở T-041/T-081 (access token hết hạn sau 15 phút, gây `401` liên tục khi tự kiểm thử).
  - `TokenAuthenticator.kt` mới (implement `okhttp3.Authenticator`, không phải Interceptor — chỉ Authenticator mới được OkHttp gọi lại kèm request gốc để retry sau 401) — khi 1 request có JWT nhận `401`: khoá `synchronized` để tránh nhiều request song song cùng gọi refresh, gọi `POST /auth/refresh` qua `RefreshApiService` (Retrofit **riêng**, dùng `OkHttpClient` **không** gắn `TokenAuthenticator` — tránh đệ quy vô hạn khi chính `/auth/refresh` cũng trả 401), cập nhật `TokenProvider` + persist vào DataStore, rồi retry request gốc với token mới. Nếu refresh token cũng hết hạn (>30 ngày không mở app) → xoá sạch phiên (access/refresh token + email/id cache) → `AuthRepository.isLoggedIn` tự chuyển `false`.
  - `NetworkModule.kt`: thêm qualifier `@RefreshHttpClient` cho `OkHttpClient` riêng của `RefreshApiService` (tránh xung đột binding với `OkHttpClient` chính), gắn `.authenticator(tokenAuthenticator)` vào client chính.
  - `SessionViewModel.kt` mới (tạo 1 lần ở `CozyPomoNavHost`, cùng tầng với `currencyViewModel`/`cheatViewModel`) quan sát `authRepository.isLoggedIn` — khi `TokenAuthenticator` phải xoá phiên vì refresh token cũng hết hạn (không phải người dùng tự bấm Đăng xuất), Flow này tự chuyển `false` và `CozyPomoNavHost` tự điều hướng về Login qua `onLogout()`, không cần người dùng tự nhận ra rồi vào Cài đặt bấm Đăng xuất thủ công.
  - **Chưa verify trên emulator/production thật** (chỉ xác nhận build thành công) — cần test thật: để access token hết hạn tự nhiên (hoặc chỉnh giờ máy) rồi gọi 1 API cần JWT, xác nhận tự refresh mà không văng về Login; và test refresh token hết hạn (JWT_REFRESH_SECRET đổi hoặc token thu hồi) để xác nhận tự điều hướng Login đúng.
- **T-094 — Màn Thống kê (S-06) thật.** `StatsViewModel.kt` mới: gọi `GET /stats/range` (7 ngày gần nhất theo ngày local máy, không cố khớp tuyệt đối UTC với backend) + `GET /stats/summary`, tự điền 0 cho ngày không có phiên nào (backend chỉ trả ngày CÓ dữ liệu). `StatsScreen.kt` viết lại hoàn toàn (trước là `PlaceholderScreen`): thẻ streak (icon lửa), 2 ô thống kê phiên hoàn thành/bỏ cuộc trong 7 ngày, biểu đồ cột tự vẽ bằng `Box` lồng nhau (không dùng thư viện chart ngoài) cho phút tập trung mỗi ngày, cột hôm nay tô đậm hơn.
- **T-095 — Cài đặt (S-07) phần còn lại — trừ Sao lưu Google Drive theo yêu cầu.**
  - `SettingsViewModel.kt`: thêm `GET`/`PATCH /settings` (đã có sẵn ở backend từ T-010, Android chưa gọi tới) — cấu hình thời gian tập trung/nghỉ mặc định (`Slider`, chỉ PATCH khi thả tay qua `onValueChangeFinished`, kéo mượt không gọi mạng liên tục) + Strict Mode (`Switch`) + chủ đề âm thanh (`FilterChip` 4 lựa chọn cứng: Mặc định/Mưa/Rừng/Lo-fi — chỉ lưu lựa chọn, **chưa** phát âm thanh thật, đó là T-042 riêng chưa làm). Thêm Kho đồ: `GET /inventory` (đã có từ T-076 nhưng chỉ dùng để check sở hữu ở Cửa hàng, chưa có UI riêng) + `PATCH /inventory/:id/equip` (có từ T-008, chưa từng có UI gọi tới) — liệt kê bình/nhạc đã mua, nút "Dùng"/"Đang dùng".
  - `SettingsScreen.kt`: thêm 3 `SettingsSection` mới ("Cấu hình phiên tập trung", "Âm thanh", "Kho đồ") giữa "Tài khoản" và "Số dư" đã có.
  - **Chủ động KHÔNG làm** S-07a (Sao lưu & Đồng bộ Google Drive, `SyncRepository`) theo đúng yêu cầu — khác các mục "chưa làm" khác trong plan này, đây là quyết định phạm vi, không phải để dành làm sau.
  - **Chưa verify trên emulator** (chỉ xác nhận build thành công) — cần test thật: kéo Slider thời gian tập trung/nghỉ xác nhận PATCH đúng giá trị sau khi thả tay, bật/tắt Strict Mode, chọn chủ đề âm thanh, mua 1 bình/nhạc ở Cửa hàng rồi xác nhận hiện đúng trong Kho đồ + bấm "Dùng" chuyển đúng trạng thái `equipped`.

- **T-096 — Fix thông báo (Snackbar) bị thanh điều hướng che khuất, đổi sang modal.** Dev1002 chụp ảnh cho thấy Snackbar kết quả cheat ("Đã kéo phiên đang chạy về còn ~5s") bị đè bởi bottom nav + thanh điều hướng hệ thống 3 nút.
  - Nguyên nhân: cả 2 Snackbar trong app (cheat message ở `CozyPomoNavHost.kt`, kết quả mua hàng ở `ShopScreen.kt`) đều render trong 1 `Box` nổi tự do `fillMaxSize().padding(16.dp)` neo `Alignment.BottomCenter` — không tính inset của bottom nav (Scaffold) lẫn thanh điều hướng hệ thống (edge-to-edge, xem T-080 gặp vấn đề tương tự với version text ở Splash), nên bị vẽ đè lên/dưới các thanh đó.
  - Fix: thêm `MessageDialog.kt` dùng chung (`ui/common/`) — modal `Dialog` nhỏ gọn (card bo góc, tonalElevation, nút "Đóng"), thay cho cả 2 Snackbar cũ. Dialog render trong 1 window Android riêng nên luôn hiện giữa màn hình, không bao giờ bị các thanh điều hướng che, và "tinh tế" hơn kiểu banner đáy màn hình theo đúng yêu cầu.
  - **Chưa verify trên emulator** (chỉ xác nhận build thành công) — cần test thật: kích hoạt 1 lệnh cheat và 1 lần mua hàng ở Cửa hàng, xác nhận modal hiện giữa màn hình, không bị che bởi bottom nav/thanh điều hướng hệ thống.

- **T-097 — Push + release `app-v1.20260724.001` (versionCode 3, versionName 0.3.0).** Dev1002 xác nhận sẽ tự kiểm thử thủ công (không cần Claude verify trên emulator trước) và yêu cầu push + release luôn.
  - 3 commit tách riêng theo đúng thói quen repo (code / `plan.md` / bump version): `a2d43f0` (toàn bộ T-091→T-096), `8d9867a` (`plan.md`), `c2cea5e` (`versionCode 2→3`, `versionName 0.2.0→0.3.0`) — push thẳng lên `main` (fast-forward, không qua PR, đúng thói quen của repo).
  - Tạo GitHub Release `app-v1.20260724.001` qua `gh release create` → `android-release.yml` tự chạy — xác nhận thành công qua `gh run watch` (job `build` xanh, các bước "Build AAB + APK (release)" và "Upload AAB + APK to release" đều pass) và `gh release view` thấy đủ 2 asset `app-release.aab`/`app-release.apk`.
  - **Lưu ý:** đây chỉ là build/ký/upload asset qua CI — **chưa** đăng lên Play Console (Nhóm E). Dev1002 bắt đầu tự kiểm thử `app-v1.20260724.001` trên emulator ngay sau đó, phát hiện T-098 bên dưới.

- **T-098 — Fix: card "Nhận thưởng bằng"/slider ấp trứng đè lên nút BẮT ĐẦU khi chọn trứng.** Phát hiện bởi Dev1002 khi tự kiểm thử `app-v1.20260724.001` trên emulator (chụp ảnh minh hoạ) — chưa kịp release trước khi build tiếp `app-v1.20260724.001`.
  - Nguyên nhân: `HomeScreen.kt` — Column giữa (`Modifier.weight(1f)`, `verticalArrangement = Arrangement.Center`) không có `verticalScroll` lẫn clip; khi chọn trứng để ấp, thêm khối "Dành cho ấp trứng: X%" + Slider khiến tổng chiều cao nội dung vượt quá không gian được cấp (nhất là màn hình thấp) — Compose không tự clip nội dung tràn, nên phần tràn xuống dưới **đè lên** nút "BẮT ĐẦU" (Button) render ngay sau Column đó.
  - Fix: (1) thêm `.verticalScroll(rememberScrollState())` vào Column giữa — nội dung tràn giờ cuộn được thay vì đè lên nút, không bao giờ che nhau nữa dù thêm bao nhiêu control trong tương lai. (2) Gom "Nhận thưởng bằng" + "Dành cho ấp trứng" vào chung 1 `Surface` card bo góc (`surfaceContainer`, giống ngôn ngữ hình ảnh `SettingsSection`), ngăn cách bằng `HorizontalDivider` — thay vì 2 khối rời rạc nổi tự do, nhìn gọn và "tinh tế" hơn hẳn. (3) Giảm nhẹ khoảng cách Spacer quanh đồng hồ đếm ngược (28dp→20dp, 24dp→16dp) để bớt tràn màn hình ngay từ đầu.
  - **Đã verify trên emulator:** build lại `assembleDebug`, cài đè (`adb install -r`), chọn trứng "Trứng Bí Ẩn" để ấp — xác nhận card "Dành cho ấp trứng: 100%" hiện đầy đủ, có `HorizontalDivider` ngăn cách rõ ràng, cuộn được, không còn đè lên nút "BẮT ĐẦU". **Chưa build lại `assembleRelease`/release/push** — đang chờ Dev1002 test tiếp trên emulator debug build này trước.

- **T-099 — Kho đồ thành màn riêng (5th tab), font mới, gọn lại header Trang chủ.** Yêu cầu tiếp theo của Dev1002 kèm 2 ảnh chụp minh hoạ (bubble cheat che nhau, header trống trải).
  - **Kho đồ tách khỏi Cài đặt/Khu rừng, thành tab thứ 5.** `ui/inventory/` mới (`InventoryViewModel.kt` + `InventoryScreen.kt`) — 3 tab con (Bình/Trứng/Âm thanh) qua `FilterChip`, lưới thẻ 2 cột (`LazyVerticalGrid`) thay cho danh sách hàng rời rạc cũ. Thẻ Bình/Âm thanh trang bị được (bo góc, nổi bật `primaryContainer` + tick góc khi đang dùng, chạm để đổi — 1-chọn-1 theo danh mục, backend tự tắt món khác cùng loại). Thẻ Trứng chỉ xem tiến trình (thanh `LinearProgressIndicator`), không có khái niệm trang bị. Xoá "Kho đồ" khỏi `SettingsScreen`/`SettingsViewModel` (T-039 cũ) và xoá tab "Kho Trứng" khỏi `ForestScreen`/`ForestViewModel` (T-084 cũ) — cả 2 nơi cũ đều bị thay thế hoàn toàn, không giữ trùng lặp. `CozyPomoDestination` thêm `Inventory` (icon `Inventory2`) — bottom nav giờ 5 tab.
  - **Bug thật gặp khi tự kiểm thử — race condition khi trang bị:** `InventoryViewModel.equip()` ban đầu nhận thẳng `InventoryItemDto` làm tham số — lambda `onClick` giữ closure của item tại thời điểm render. Chạm 2 lần liên tiếp trong lúc PATCH đầu tiên chưa trả về (round-trip ~600-800ms) khiến lần chạm thứ 2 vẫn đọc `equipped=false` cũ từ closure, gọi thêm 1 lần toggle nữa và tắt luôn về "0 món nào được trang bị" — tái hiện được y hệt hiện tượng Dev1002 gặp trước đó (mọi bình đều "chưa dùng" dù seed data mặc định có 1 món equipped). Fix: `equip(itemId: String)` đọc lại trạng thái MỚI NHẤT từ `_uiState` theo id thay vì dùng tham số object; thêm `pendingEquipId` chặn hẳn chạm thêm (dù cùng món hay món khác) trong lúc còn request bay, thẻ đang chờ mờ đi + hiện `CircularProgressIndicator` nhỏ góc trên-phải thay vì tick.
  - **Áp dụng bình đang trang bị lên hình bình ở Trang chủ.** `ShopItem` không có cột màu riêng cho JAR_SKIN (kiểm tra `schema.prisma` — chỉ có `name`/`priceCoin`/`eggTypeId`) nên không thể lấy màu thật từ backend; `jarTintFor(name)` mới (`JarMark.kt`) suy màu ổn định từ hash tên món, lấy trong `SPECIES_PALETTE` sẵn có (không bịa bảng màu mới) — cùng 1 tên luôn ra cùng 1 màu. `JarMark` thêm tham số `jarTint: Color?` áp vào màu thân bình (mặc định `null` = giữ nguyên `primary` như cũ). `HomeViewModel` tải `equippedJarSkinName` qua `GET /inventory`, lọc `JAR_SKIN && equipped`.
    - **Bug thật gặp khi tự kiểm thử — hình bình không tự cập nhật khi quay lại tab.** Trang bị bình mới ở Kho đồ trong lúc KHÔNG đứng ở Trang chủ, rồi quay lại tab đó — hình bình vẫn giữ màu cũ, phải khởi động lại app mới thấy đúng (xác nhận qua logcat: không có lệnh `GET /inventory` nào từ `HomeViewModel` sau khi trang bị, dù `CollectionEventBus` đã `notifyChanged()` và listener đã đăng ký đúng trong `init{}` — nghi liên quan tới cách Navigation Compose lưu/khôi phục `ViewModelStore` giữa các tab dùng `saveState`/`restoreState`, chưa xác định được gốc rễ chính xác). Fix theo hướng chắc chắn đúng thay vì tiếp tục đào sâu nguyên nhân: thêm `HomeViewModel.refreshEquippedJarSkin()` (public) gọi từ `LaunchedEffect(Unit)` ngay đầu `HomeScreen` — composable này được Compose tạo lại mỗi lần tab Trang chủ được vào lại (khác với `ViewModel` vẫn sống xuyên suốt), nên đảm bảo luôn tải đúng dữ liệu mới nhất bất kể `collectionEventBus` có tới kịp hay không. Giữ nguyên listener `collectionEventBus` cũ làm lớp dự phòng thứ 2 (cập nhật ngay nếu đang đứng sẵn ở Trang chủ lúc trang bị đổi từ nơi khác, ít tốn hơn phải rời-rồi-quay-lại).
  - **Gọn lại header Trang chủ.** Trước đây `CozyPomoNavHost` (bubble số dư) và `HomeScreen` (icon Cài đặt) mỗi bên tự vẽ 1 hàng riêng xếp chồng lên nhau, tạo khoảng trống thừa giữa 2 hàng liền kề (Dev1002 khoanh đỏ trong ảnh chụp). Gộp về 1 hàng DUY NHẤT trong `topBar` dùng chung của `CozyPomoNavHost` (icon Cài đặt `Alignment.CenterStart` + bubble số dư `Alignment.CenterEnd`) — vừa gọn hơn, vừa cho mở Cài đặt từ MỌI tab thay vì chỉ Trang chủ. `HomeScreen` bỏ hẳn tham số `onOpenSettings` và Row icon riêng.
  - **Đổi font toàn app: Nunito → Baloo 2.** Dev1002 yêu cầu font "mập mạp" hơn, vẫn hỗ trợ tiếng Việt, hợp không khí game. Xác minh qua Google Fonts METADATA.pb (`ofl/baloo2/`) có subset `vietnamese` trước khi đổi (cùng mức cẩn thận đã làm với Nunito ở T-071) — tải bản variable font (`res/font/baloo2.ttf`, trục `wght` 400-800), viết lại `Type.kt` theo đúng pattern cũ (`FontVariation.Settings` per-weight). Xoá hẳn `nunito.ttf` (không còn nơi nào dùng).
  - **Đã verify đầy đủ trên emulator** (không chỉ code review): Kho đồ hiện đúng 3 tab + lưới thẻ; trang bị 1 lần sạch (không còn race condition) → tick + nền xanh hiện đúng; quay lại Trang chủ → hình bình đổi màu đúng theo món vừa trang bị (test 2 màu khác nhau: tan "Bình Gốm Nung Mộc" và hồng "Bình Pha Lê Tuyết", cả 2 đều lên đúng ngay khi vào lại tab); header gọn còn 1 hàng; font Baloo 2 hiện rõ khác biệt (chunky/rounded) ở mọi màn đã xem qua (Splash, Trang chủ, dialog Bỏ cuộc, Kho đồ).

- **T-100 — Push + release `app-v1.20260724.002` (versionCode 4) + đồng bộ lại tài liệu.**
  - 3 commit (code T-098/T-099, `plan.md`, bump version) push thẳng `main` → tạo GitHub Release `app-v1.20260724.002` → `android-release.yml` xác nhận xanh (`gh run watch`), `gh release view` thấy đủ `app-release.aab`/`app-release.apk`.
  - **Đồng bộ lại `docs/technical-spec.md` + `docs/wireframes/use-case-flow.html`** cho khớp bản build thật — 2 tài liệu này vẫn giữ nguyên trạng thái từ lúc thiết kế ban đầu (2026-07-22, mọi thứ "⬜ Chưa làm"), trong khi thực tế toàn bộ Nhóm A đã xong từ lâu và kinh tế game đã đổi hẳn (2 tiền tệ, trứng ấp dần, 5 tab). Cập nhật: Screen List thêm S-07b Kho đồ, đánh dấu S-07a "đã bỏ khỏi phạm vi" (khác "chưa làm"); Function List sửa lại chữ ký hàm theo economy v2/v3 thật; wireframe HTML thêm mockup Kho đồ + 5-tab nav ở mọi màn, sửa mockup Trang chủ (2 số dư + chip chọn thưởng), mockup Chọn Trứng (theo tiến trình thay vì số lượng), bỏ hẳn mockup Sao lưu, bảng đối chiếu cuối trang cập nhật hết trạng thái "Đã xong".
  - Commit riêng (không gộp vào T-097→T-099) rồi push thẳng `main` — docs-only, không cần release Android mới.

- **T-101 — Fix Splash hiện sai version: đổi sang đúng tag GitHub Release thật.**
  - Nguyên nhân: Splash hiện `"v${BuildConfig.VERSION_NAME}"` (VD "v0.4.0") — chỉ là `versionName` khai báo tay trong `build.gradle.kts`, không liên quan gì tới tag Release thật trên GitHub (VD `app-v1.20260724.002`, đặt tên theo ngày+lần release trong ngày). 2 hệ đánh số này độc lập nhau nên không khớp — Dev1002 xem trên GitHub Releases thấy 1 tag nhưng app lại hiện số khác hẳn.
  - Fix: thêm `BuildConfig.RELEASE_TAG` mới trong `app/build.gradle.kts`, đọc từ biến môi trường `RELEASE_TAG` (cùng cơ chế `envOrProp` đã dùng cho keystore secrets) — CI (`android-release.yml`) truyền `RELEASE_TAG: ${{ github.event.release.tag_name }}` vào bước build release, nên APK/AAB build ra bake sẵn đúng tag Release thật đã tạo ra nó. Build local/dev không có biến này thì fallback về `"v$versionName"` như cũ (không có tag thật để hiện). `SplashScreen.kt` đổi sang hiện `BuildConfig.RELEASE_TAG` thay vì `"v${VERSION_NAME}"`.
  - **Đã verify trên emulator** — build debug với `RELEASE_TAG=app-v1.20260724.002 ./gradlew :app:assembleDebug` mô phỏng đúng 1 lần build release thật, xác nhận Splash hiện đúng `app-v1.20260724.002` ở góc dưới màn hình.

- **T-102 — Màn Giới thiệu mới (icon (i) ở Cài đặt) — mô tả app, tác giả Dev1002, phiên bản, nút Buy Me a Coffee.**
  - `ui/about/AboutScreen.kt` mới: icon bình ấp (`JarMark`, đồng bộ hình ảnh với Splash), tên "CozyPomo", phiên bản (`BuildConfig.RELEASE_TAG`, khớp T-101), "Tác giả: Dev1002", đoạn mô tả ngắn tự biên soạn về ý tưởng ấp trứng/nuôi khu rừng, và nút "Buy Me a Coffee" (màu `secondary`, icon cốc cà phê) mở `https://buymeacoffee.com/nhutnm` qua `Intent.ACTION_VIEW`.
  - Bố trí: icon (i) mới ở `actions` của `TopAppBar` màn Cài đặt (`SettingsScreen.kt`, cạnh nút quay lại) — không thêm vào header dùng chung toàn app (`CozyPomoNavHost`) để tránh làm header vừa gọn lại ở T-099 bị đông icon trở lại; đặt trong Cài đặt vẫn dễ tìm và đúng quy ước phổ biến (Settings → About) trên hầu hết app Android.
  - Route `about` mới trong `CozyPomoNavHost.kt`, cùng tầng với `settings`.
  - **Đã verify trên emulator:** chạm icon (i) ở Cài đặt → mở đúng màn Giới thiệu với đủ nội dung; chạm "Buy Me a Coffee" → xác nhận mở đúng Chrome với Intent thật (không crash) — không đi tiếp qua bước chấp nhận ToS/điều hướng thật tới trang, chỉ xác minh Intent bắn đúng.
  - **Chưa commit/push/release** — đang chờ Dev1002 xác nhận.

- **T-103 — Chuẩn bị phát hành Play Store (Nhóm E) + README chuyên nghiệp.** Hạ tầng release (keystore, CI/CD ký app) đã xong từ trước — Dev1002 xác nhận phần còn lại chỉ là nội dung/thủ tục.
  - **Feature graphic** (`docs/play-store/feature-graphic-1024x500.png`): dựng bằng ImageMagick từ logo chính thức + tagline font Baloo 2 trên nền ivory, xuất đúng 1024×500 PNG 24-bit không kênh alpha (`identify -verbose` xác nhận `Type: TrueColor`, đúng yêu cầu Google).
  - **5 ảnh chụp màn hình thật** (`docs/play-store/screenshots/`) từ emulator (Trang chủ, Khu rừng, Cửa hàng, Kho đồ, Thống kê). Ảnh gốc 1080×2340 (tỉ lệ 2.167:1) vi phạm giới hạn tỉ lệ dài:ngắn ≤ 2:1 của Google — cắt bớt thanh điều hướng hệ thống ở đáy (không phải nội dung app) còn 1080×2130 (tỉ lệ 1.972:1) bằng `magick -crop`.
  - **Chính sách bảo mật** (`backend/legal/privacy-policy.html`): trang HTML tiếng Việt đầy đủ (dữ liệu thu thập, không chia sẻ bên thứ 3, không quảng cáo/theo dõi, quyền xoá dữ liệu trong 30 ngày, liên hệ). Mount qua `useStaticAssets` mới trong `backend/src/main.ts` (`prefix: '/legal'`), `Dockerfile` thêm `COPY legal ./legal` (đúng bài học đã rút ra ở T-067 về quên copy thư mục static).
  - **`docs/play-store/data-safety-form-guide.md`** — bảng câu trả lời tham khảo cho Data Safety form (chỉ điền qua UI Play Console): chỉ Email + App interactions là "có thu thập", không chia sẻ bên thứ 3 nào; đơn giản hơn dự kiến ban đầu vì Google Drive Sync (S-07a) đã bị bỏ khỏi phạm vi từ T-095.
  - **`docs/play-store/content-rating-guide.md`** — bảng câu hỏi IARC; **lưu ý quan trọng**: cơ chế roll ngẫu nhiên có trọng số khi ấp trứng về bản chất là "simulated gambling" nhẹ, nên câu hỏi tương ứng phải trả lời **Yes** (kèm giải thích không có tiền thật/phần thưởng thật) — trả lời sai/bỏ sót có thể khiến Google gỡ app sau khi review thủ công. Dự đoán rating vẫn ở mức thấp nhất (ESRB Everyone/PEGI 3/USK 0).
  - **`docs/play-store/README.md`** — tổng hợp toàn bộ, gồm checklist các bước thủ công còn lại trong Play Console (dán URL Privacy Policy, điền 2 form, upload assets, tạo app + track).
  - **README.md gốc viết lại chuyên nghiệp**: giới thiệu sản phẩm, ảnh chụp màn hình thật, danh sách tính năng khớp bản build hiện tại, bảng tech stack, cấu trúc repo, hướng dẫn chạy dev cho cả `app/` và `backend/`, tổng quan release/CI-CD, bảng liên kết tài liệu, mục Giấy phép (chưa chọn license mã nguồn mở — không tự ý chọn thay Dev1002) và tác giả kèm link Buy Me a Coffee.
  - **Đã deploy backend** (xem T-104) — `https://cozyapi.nhutnm.id.vn/legal/privacy-policy.html` xác nhận trả về `200` sau release.

- **T-104 — Fix "Thông tin ứng dụng" ở Cài đặt hiện sai phiên bản + push/release cả app lẫn backend.** Dev1002 phát hiện màn Cài đặt cũng hiện sai phiên bản giống lỗi Splash đã sửa ở T-101 — bug T-101 mới chỉ sửa `SplashScreen.kt`, bỏ sót `SettingsViewModel.kt`.
  - Nguyên nhân: `SettingsUiState.versionName` (dùng để hiện "Phiên bản ${uiState.versionName}" trong section "Thông tin ứng dụng") vẫn đọc `BuildConfig.VERSION_NAME` (VD "0.4.0", số tự khai trong `build.gradle.kts`) thay vì `BuildConfig.RELEASE_TAG` (tag Release thật trên GitHub) — cùng gốc rễ đã sửa ở Splash nhưng do 2 nơi hiện version độc lập nhau nên T-101 không tự động fix chỗ này.
  - Fix: đổi `versionName: String = BuildConfig.VERSION_NAME` → `= BuildConfig.RELEASE_TAG` trong `SettingsViewModel.kt` — 1 dòng, không đổi UI/logic khác (5-tap vào dòng này vẫn mở cheat bubble như cũ, không phụ thuộc nội dung text).
  - **Push + release cả 2 phía theo yêu cầu:**
    - Android: 3 commit (`40370ee` code T-101/T-102/T-103/T-104 gộp, `63c5ee1` docs Nhóm E + README, `67deead` bump `versionCode 4→5`/`versionName 0.4.0→0.5.0`) push thẳng `main` → GitHub Release `app-v1.20260724.003` → `android-release.yml` xanh (`gh run watch`), asset `app-release.aab`/`app-release.apk` xác nhận có đủ.
    - Backend: cùng đợt code T-101/T-103 (`main.ts` mount `/legal`, `Dockerfile` copy `legal/`) đã nằm trong commit `40370ee` ở trên → GitHub Release `backend-v1.20260724.001` → `backend-deploy.yml` xanh (`build-and-push` + `deploy` qua SSH, `reseed-production` bị skip vì không đổi schema — đúng như thiết kế). Xác nhận `curl -o /dev/null -w "%{http_code}" https://cozyapi.nhutnm.id.vn/legal/privacy-policy.html` trả về `200`.
  - **Push kèm toàn bộ artifact Nhóm E (wireframe cập nhật từ T-100, feature graphic, 5 screenshot, 2 guide Data Safety/Content Rating, README.md mới)** lên GitHub để lưu trữ, theo đúng yêu cầu — không còn nằm riêng ở working tree nữa.
  - **Chưa verify trên thiết bị thật/emulator lần build này** (chỉ xác nhận build CI xanh + compile sạch trước khi commit) — Dev1002 tự kiểm thử `app-v1.20260724.003` khi tiện.

- **T-105 — Fix nghiêm trọng: bấm Trang chủ/tab khác ở bottom nav không có tác dụng khi đang ở Cài đặt.** Dev1002 báo lỗi ngay sau khi cài `app-v1.20260724.003` — vào Cài đặt rồi bấm Trang chủ (hoặc tab khác) ở thanh điều hướng dưới không chuyển màn.
  - **Tái hiện được 100% trên emulator** bằng `adb shell uiautomator`/`input tap` + screenshot: Trang chủ → chạm icon Cài đặt (gear) → chạm 1 tab BẤT KỲ (VD Khu rừng) → chạm Trang chủ ở bottom nav → **quay lại đúng màn Cài đặt** thay vì Trang chủ. Test bằng `adb logcat` với log tạm thời in `navController.currentBackStack` trước/sau mỗi lần `navigate()` xác nhận: `navigate("home", popUpTo(homeId){saveState=true}; launchSingleTop=true; restoreState=true)` khi gọi từ Khu rừng trả về back stack `[home, settings]` thay vì `[home]` — tức bản thân lời gọi navigate tới đúng "home" lại tự ý restore nhầm sang "settings".
  - **Nguyên nhân gốc: bug thật của thư viện Navigation-Compose** (`androidx.navigation:navigation-compose:2.9.8`) khi kết hợp `popUpTo(startDestinationId) { saveState = true }` + `restoreState = true` — nếu có bất kỳ route NÀO ngoài các tab (ở đây là `settings`/`about`, được push bằng `navController.navigate(route)` trần, không qua cùng cơ chế popUpTo/saveState của bottom nav) từng bị pop-with-saveState (xảy ra khi bấm 1 tab từ Cài đặt), trạng thái đã lưu đó bị "registry" của NavController lẫn lộn — lần sau đó `navigate()` **đúng về route trùng với `startDestination`** ("home") lại bị restore nhầm sang entry đã lưu trước đó ("settings") thay vì hiện home. Vì `settings`/`about` không nằm trong `CozyPomoDestination.entries` (5 tab), chúng không được quản lý bởi cùng vòng đời saveState/restoreState mà tab dùng, nên việc chúng "lọt" vào registry qua đường pop-up-to của tab là nguồn gốc lỗi.
  - **Fix:** bỏ hẳn `saveState`/`restoreState` khỏi `NavigationBarItem.onClick` trong `CozyPomoNavHost.kt` — chỉ giữ `popUpTo(startDestinationId)` (không `saveState`) + `launchSingleTop = true`, tránh hoàn toàn registry lỗi này. Đồng thời thêm `launchSingleTop = true` cho 2 lời gọi `navigate(SettingsRoute)`/`navigate(AboutRoute)` (icon Cài đặt + icon Giới thiệu) để tránh chồng nhiều instance nếu bấm liên tiếp. Đánh đổi: chuyển tab giờ tạo lại ViewModel của tab đó (gọi lại API) thay vì giữ nguyên qua `saveState` — chấp nhận được vì các màn đã tự refetch dữ liệu khi vào lại (cùng tinh thần với cách T-099 tự fetch lại jar tint qua `LaunchedEffect(Unit)` thay vì dựa vào cơ chế restoreState).
  - **Đã verify đầy đủ trên emulator sau fix** (dùng đúng kịch bản tái hiện được lỗi ở trên, cộng thêm test toàn diện): Cài đặt → Khu rừng → Trang chủ hiện đúng Trang chủ (không còn quay lại Cài đặt); tương tự từ màn Giới thiệu → Cửa hàng cũng đúng; bấm lần lượt cả 5 tab trực tiếp từ Cài đặt đều đúng; nút Back hệ thống từ 1 tab bất kỳ quay đúng về Trang chủ (start destination) như hành vi chuẩn, không bị kẹt hay thoát app ngoài ý muốn.
  - Bump `versionCode 5→6`, `versionName 0.5.0→0.6.0`. Backend không có thay đổi gì kể từ `backend-v1.20260724.001` (xác nhận qua `git diff backend-v1.20260724.001 HEAD -- backend/` rỗng) nên chỉ release Android, không tạo release backend mới.

### Android — T-042 SoundManager/NotificationManager + T-043 WorkManager outbox offline→online (2026-07-25)

Xây 2 chức năng còn lại của Nhóm A theo đúng mục 3.9 `docs/technical-spec.md` + thiết kế outbox ở `docs/technical-spec.md` mục 4.4/`POST /sync/batch`. **Chưa verify trên emulator/thiết bị thật** — chỉ mới xác nhận qua Gradle build thật (`./gradlew :app:assembleDebug` xanh, KSP Room+Hilt validate graph không lỗi), **chưa commit/push**.

- **T-042 — SoundManager/NotificationManager.**
  - **Âm thanh sinh mới bằng `ffmpeg`** (không dùng nhạc có bản quyền, cùng cách làm T-080): `res/raw/completion_chime.wav` (tiếng "ting" 0.9s, 2 tần số bell), `res/raw/ambient_rain.ogg`/`ambient_forest.ogg`/`ambient_lofi.ogg` (noise lọc dải tần khác nhau cho mưa/rừng, pad hợp âm sine cho lo-fi, 16–20s loop, encode Vorbis nhẹ ~80–190KB/file).
  - **`data/sound/SoundManager.kt`** (Hilt singleton app-scoped): `playAmbientTrack(trackId)` dùng Media3 `ExoPlayer` (`repeatMode = REPEAT_MODE_ONE`, `AudioAttributes` USAGE_MEDIA + `handleAudioFocus=true`) — `"default"` hoặc giá trị lạ = không phát gì; `stopAmbientTrack()`; `playCompletionChime()` dùng `SoundPool` (độ trễ thấp, hợp âm ngắn). Sống được xuyên phiên vì `TimerForegroundService` (specialUse FGS) giữ process không bị hệ thống kill.
  - **`data/notification/SessionNotifier.kt` + `SessionEndReceiver.kt`**: lớp bảo hiểm độc lập báo hết giờ ngay cả khi màn hình khoá VÀ `TimerForegroundService` bị kill (hiếm) — `AlarmManager.setAndAllowWhileIdle` (cố ý dùng bản KHÔNG exact để khỏi cần xin quyền `SCHEDULE_EXACT_ALARM` Android 12+, chấp nhận trễ vài phút trong Doze vì đây chỉ là lớp dự phòng, đường chính vẫn là service đếm `SystemClock.elapsedRealtime()`). `SessionEndReceiver` (registered trong Manifest) chỉ hiện thông báo (kênh `IMPORTANCE_HIGH` + sound = `completion_chime`), KHÔNG đụng Room/gọi API — tránh đua với service.
  - **Nối vào `TimerRepository`**: `startSession` gọi `apiService.getSettings().soundTheme` (fallback `"default"` nếu lỗi mạng) rồi `playAmbientTrack`, đồng thời `scheduleSessionEndNotification`. `completeSession` huỷ báo động dự phòng + dừng nhạc nền ngay đầu hàm (đường chính đang xử lý), phát chime sau khi có kết quả. `giveUpSession` dừng nhạc nền + huỷ báo động (không chime).
  - Thêm dependency `androidx.media3:media3-exoplayer`/`media3-common` (`1.10.1`).

- **T-043 — WorkManager đồng bộ offline→online.**
  - **Bảng `sync_outbox` mới** (`data/local/sync/SyncOutboxEntity.kt` + `SyncOutboxDao.kt`, `CozyPomoDatabase` version 3→4) — mỗi dòng chỉ lưu `localSessionId` + `type` (`session_create`/`session_complete`/`session_give_up`), KHÔNG đóng băng payload tại thời điểm ghi — `SyncOutboxWorker` đọc lại state mới nhất từ bảng `sessions` ngay lúc flush.
  - **`TimerRepository`**: mỗi nhánh `runCatching{}` gọi backend thất bại (create/complete/give-up) giờ ghi 1 dòng outbox + báo `SyncOutboxScheduler.enqueueFlush()` thay vì im lặng bỏ qua như trước — đây chính là lỗ hổng tin cậy T-030 đã ghi chú từ đầu ("phiên hoàn thành lúc mất mạng").
  - **`data/network/SyncDto.kt`** + `ApiService.syncBatch()` — request/response khớp `backend/src/sync/dto/sync-batch.dto.ts` (`payload` kiểu `JsonObject` tự do vì mỗi `type` có field khác nhau).
  - **`data/sync/SyncOutboxWorker.kt`** (`@HiltWorker`, `CoroutineWorker`): xử lý theo trình tự tạo (`session_create` trước). Với `session_complete`/`session_give_up`, backend KHÔNG hỗ trợ tra theo `clientEventId` (chỉ `session_create` idempotent theo field này) nên cần `remoteId` thật — nếu dòng `session_create` cùng phiên vẫn chưa xong (cũng đang mất mạng), tạm bỏ qua dòng phụ thuộc này, đợi lượt chạy sau. Gửi các event đã resolve được trong 1 lần gọi `POST /sync/batch`, parse lại `data.id` để cập nhật `remoteId` cho dòng `session_create` thành công, xoá khỏi outbox cả dòng `"ok"` lẫn `"error"` (lỗi phía backend gửi lại mãi cũng không tự hết). Network lỗi toàn bộ → `Result.retry()` (WorkManager tự backoff).
  - **`data/sync/SyncOutboxScheduler.kt`**: `enqueueFlush()` (one-time, `NetworkType.CONNECTED`, gọi ngay sau khi ghi outbox) + `schedulePeriodicFlush()` (định kỳ 30 phút, lưới an toàn nếu app bị đóng ngay sau khi ghi outbox trước khi kịp enqueue).
  - **`CozyPomoApp.kt`**: implement `Configuration.Provider` + inject `HiltWorkerFactory` (bắt buộc để `@HiltWorker` hoạt động), gọi `schedulePeriodicFlush()` ở `onCreate()`.
  - Thêm dependency `androidx.work:work-runtime-ktx` (`2.11.2`), `androidx.hilt:hilt-work` (`1.4.0`) + processor `androidx.hilt:hilt-compiler` qua KSP. Thêm quyền `ACCESS_NETWORK_STATE` (WorkManager cần để theo dõi ràng buộc mạng) + `<receiver>` cho `SessionEndReceiver`.

- **Đã verify:** `./gradlew :app:compileDebugKotlin` và `./gradlew :app:assembleDebug` chạy xanh hoàn toàn (bao gồm `kspDebugKotlin` — Room schema + Dagger/Hilt graph validate không lỗi qua cả 2 processor cùng lúc, và `hiltJavaCompileDebug` — sinh component Hilt thật thành công), APK debug build ra đóng gói đủ 5 file audio trong `res/raw/`. **Chưa test hành vi thật trên emulator/thiết bị** (phát nhạc nền đúng theme, tiếng ting đúng lúc, thông báo dự phòng khi khoá màn hình, outbox gửi lại đúng sau khi mất mạng rồi có mạng lại) — cần Dev1002 hoặc phiên làm việc sau verify trước khi commit/push.
- Bump `versionCode 6→7`, `versionName 0.6.0→0.7.0` (chưa commit).

### Backend + Android — T-106→T-114: Chợ (Marketplace) + "Khu vườn của tôi" (2026-07-26)

Triển khai đầy đủ 2 nhóm tính năng theo wireframe đã duyệt + 4 câu trả lời của Dev1002 (không ép giá sàn/trần; phí giao dịch 10%; SSR/MYTHIC cần Admin duyệt; giới hạn 10 tin đăng/user, cấu hình được từ Backend). Lúc viết mục này còn **chưa commit/push** theo đúng yêu cầu — chỉ verify bằng build thật + curl thật + emulator (xem mục cuối) — **nhưng đã commit+push+release ở T-118 ngay sau đó cùng phiên (2026-07-26)**, xem mục T-118 bên dưới. Đã xác nhận vẫn còn nguyên trên `main` (commit `15111cd`, verify lại 2026-07-28): `MarketScreen.kt`/`GardenTerrain.kt`/`market.service.ts`/model `MarketListing` đều tồn tại thật. **Nhóm F/Nhóm G cũ trước đây liệt kê ở mục "Chưa làm" bên dưới đã lỗi thời — xoá khỏi đó, tính năng đã xong hoàn toàn, không phải chỉ là đề xuất.**

- **T-106 — Backend: schema + module Market.**
  - Prisma: `CollectionEntry` thêm `ownedCount` (tách khỏi `hatchCount` lịch sử — xem giải thích Nhóm F cũ bên dưới), enum `MarketItemType`/`MarketListingSellerType`/`MarketListingStatus` (gồm `PENDING_APPROVAL`/`REJECTED` cho luồng duyệt), model `MarketListing`, `GameSettings` thêm `marketFeePercent` (mặc định 10) + `maxActiveListingsPerUser` (mặc định 10, đúng câu trả lời Dev1002 — cấu hình qua AdminJS, không cần màn riêng vì `GameSettings` đã có sẵn resource), `LedgerEntry` thêm `refMarketListingId`, `LedgerReason` thêm `MARKET_BUY`/`MARKET_SELL`. Migration `20260726102646_market_and_owned_count` sinh bằng `prisma migrate diff` (đúng cách đã dùng ở T-068), **đã apply thật lên Postgres local + verify qua curl**.
  - `MarketService.createSpeciesListing`: validate `ownedCount − (tin ACTIVE/PENDING của user cho đúng loài) ≥ 1` (cho phép bán tới bản cuối cùng — không ép "phải dư" theo câu trả lời Dev1002), tự chuyển `PENDING_APPROVAL` nếu `rarity=SSR` hoặc `category=MYTHIC` do `USER` đăng (Admin đăng thì luôn `ACTIVE` ngay).
  - `MarketService.createEggListing`: validate trứng `INCUBATING`, không thuộc `Session` đang `RUNNING`.
  - `MarketService.buy`: **phát hiện + sửa 1 bug thật lúc tự test bằng curl** — bản đầu `updateMany` chốt trạng thái `SOLD` nằm NGOÀI `$transaction` chứa phần trừ tiền, nên khi `spend()` báo "không đủ Xu Lá" (rất hay gặp), tin đăng bị kẹt vĩnh viễn ở `SOLD` dù chưa ai trả tiền. Sửa bằng cách gộp `updateMany` + trừ/cộng tiền + chuyển quyền sở hữu vào chung 1 `$transaction` — lỗi giữa chừng tự rollback hết, tin đăng trở lại `ACTIVE`. Phí 10% trừ vào phần người bán USER nhận (`Math.round(priceCoin * 0.9)`), không hoàn ai (currency sink) — Admin bán thì không trừ phí (không có ai nhận). Chặn tự mua tin của chính mình.
  - **Đã verify bằng curl thật** (2 tester thật, Postgres local): tạo tin thường → `ACTIVE`; tạo tin SSR → `PENDING_APPROVAL`, không hiện ở feed công khai; duyệt qua sửa thẳng `status` (mô phỏng đúng luồng AdminJS edit-form, xem T-107) → hiện lại ở feed; mua chéo 2 tài khoản → đúng phí 10% (giá 40 → người bán nhận 36); bán tới bản cuối cùng (`ownedCount 1→0`) → `CollectionEntry` không bị xoá, vẫn hiện "đã mở khoá" ở Lưới; huỷ tin rồi thử mua lại → báo lỗi đúng; mua khi không đủ Xu Lá → tin đăng **không** bị kẹt (đã xác nhận bug trên đã hết).

- **T-107 — AdminJS: resource `MarketListing`.** Đăng ký resource chuẩn (không hạn chế field, giống `ShopItem` — Admin cần `new` để tự tạo tin `sellerType: ADMIN` bán vật phẩm siêu hiếm, và cần sửa `status` để duyệt/từ chối tin `PENDING_APPROVAL`). Thêm bản dịch tiếng Việt đầy đủ cho `MarketListing` + `GameSettings` (trước đó `GameSettings` chưa có bản dịch) vào `admin-i18n.ts`.
  - **Phát hiện lỗi hạ tầng có sẵn, không liên quan tới thay đổi lần này:** chạy AdminJS cục bộ (`node dist/main.js`, không qua Docker) bị lỗi 404 ở các file JS lõi của giao diện (`app.bundle.js`, `global.bundle.js`, `design-system.bundle.js`, `icomoon.css` — chỉ `components.bundle.js` chạy được) khiến trang `/admin` hiện trắng, không bấm được gì. Truy ra nguyên nhân: `@adminjs/express` (dependency của `@adminjs/nestjs`) viết cho Express 4.x (`peerDependencies: "express": ">=4.18.2"`), nhưng `package-lock.json` đang khoá **Express 5.2.1** — đổi engine routing lớn giữa 2 bản. Đây là lỗi **có sẵn từ trước**, không phải do thêm resource `MarketListing` (đã xác nhận `icomoon.css` — file tĩnh thuần, không liên quan gì tới resource nào — cũng 404). Không tự ý downgrade `express` (thay đổi dependency dùng chung toàn backend, ngoài phạm vi yêu cầu) — chỉ ghi nhận ở đây để Dev1002 biết, cân nhắc xử lý riêng.
  - **Do UI admin cục bộ không xem được, đã verify luồng duyệt bằng cách khác:** cập nhật thẳng `status` qua Prisma Client (đúng thao tác mà form edit chuẩn của AdminJS sẽ làm) — xác nhận tin `PENDING_APPROVAL` → `ACTIVE` sau khi "duyệt" thì hiện đúng ở `GET /market/listings` công khai. Logic nghiệp vụ đã đúng; phần còn lại (bấm được trong giao diện `/admin` thật) phụ thuộc vào việc xử lý lỗi Express 5 nói trên, không phải lỗi của T-107.

- **T-108 — Android: DTO + `ApiService` cho Chợ.** `data/network/MarketDto.kt` (`MarketListingDto`, `CreateSpeciesListingRequest`, `CreateEggListingRequest`, `BuyListingRequest`), 5 endpoint mới trong `ApiService.kt`. `CollectionEntryDto` thêm `ownedCount`. **Không tạo `MarketRepository` riêng** — khác dự tính ban đầu ở wireframe, vì rà lại codebase thấy các ViewModel khác (`ShopViewModel`, `ForestViewModel`) đều gọi thẳng `ApiService` + `runCatching`, không qua Repository riêng cho các luồng không cần offline-first — giữ nhất quán, không thêm abstraction thừa.

- **T-109 — Android UI: màn Chợ (S-08) độc lập.** `ui/market/MarketScreen.kt` + `MarketViewModel.kt` — danh sách trộn thú/thực vật/trứng, tab Tất cả/Của tôi, không có badge "Chính thức" (bỏ hẳn khỏi thiết kế theo yêu cầu trước đó) — tin Admin chỉ khác dòng "bởi CozyPomo". Dialog Xác nhận mua hiện số dư sau khi mua.

- **T-110 — Android UI: Đăng bán + quản lý tin đăng.** `ui/common/SetPriceDialog.kt` dùng chung. `SpeciesDetailDialog.kt` (S-03) thêm nút "Đăng bán" hiện khi `ownedCount > 0` (không cần "dư"), hiện rõ số bản đang sở hữu. `InventoryScreen.kt` (Kho đồ, tab Trứng) thêm nút "Đăng bán" mỗi trứng — lỗi "đang dùng trong phiên chạy" (nếu có) hiện nguyên văn từ backend. Tab "Của tôi" ở Chợ hiện đủ trạng thái (đang bán/đã bán/đã huỷ/chờ duyệt/bị từ chối), nút Huỷ cho tin `ACTIVE`.

- **T-111 — Android: bỏ tab Cửa hàng khỏi Bottom Nav, thêm FAB.** `CozyPomoDestination` còn 4 tab (Trang chủ/Khu rừng/Kho đồ/Thống kê). `ui/common/ShopMarketToggleFab.kt` — nút tròn góc phải-dưới Trang chủ (`Icons.Filled.ShoppingBasket`), bấm bung dọc 2 `SmallFloatingActionButton` kèm nhãn chữ: Cửa hàng (`Storefront`, màu primary) và Chợ (`Handshake`, màu tertiary) — animation `fadeIn/slideInVertically/scaleIn`. `ShopScreen.kt`/`MarketScreen.kt` thêm `Scaffold`+`TopAppBar` với nút quay lại (giống Cài đặt/Giới thiệu) vì giờ là route trần thay vì tab.

- **T-112 — Android: địa hình Khu vườn + thuật toán bố trí.** `ui/common/GardenLayout.kt` — `computeGardenSlots()` hàm thuần, PRNG mulberry32 (bản sao riêng công thức từ `SpeciesArt.kt` vì hàm gốc `private`), chia ô thưa + xáo trộn + jitter ±30% vị trí/góc xoay nhẹ, ổn định theo `speciesId` đã sort (không phụ thuộc thứ tự tải mạng). `ui/common/GardenTerrain.kt` — `GardenZoneBackground` vẽ bằng `Canvas` theo 4 category: cây+thân cây (FOREST), sông uốn lượn màu `--water` mới (SEA), cụm hoa (PLANT), bệ toả sáng (MYTHIC) — mảng nền bo mềm (ellipse chồng lớp), không phải khối chữ nhật cứng.
  - **Ghi chú phạm vi:** đây là bản "isometric nhẹ" (đạo cụ 2D layer + đổ bóng tạo chiều sâu, không phối cảnh isometric thật theo lưới ô thoi) — khớp phương án cân bằng thời gian/độ đẹp đã nêu ở câu hỏi 4 (Nhóm G cũ), vì Dev1002 chỉ nói "đã đồng ý wireframe" mà không chọn cụ thể giữa 2 phương án.

- **T-113 — Android: toggle Lưới ⇄ Khu vườn.** `ForestViewModel.kt` thêm `ForestViewMode` (không lưu bền). `ForestScreen.kt` thêm 2-icon toggle (`GridView`/`Yard`) cạnh tiêu đề — chế độ Lưới giữ nguyên 100% code cũ. `GardenMapView.kt` (mới, `ui/forest/`) — chỉ hiện loài `ownedCount > 0`, cuộn dọc qua 4 khu, bấm sinh vật mở đúng `SpeciesDetailDialog` có sẵn.
  - **Sửa sau phản hồi Dev1002 (test tay 12 loài trong 1 khu):** chiều cao khung mỗi khu ban đầu cố định 230dp — khi 1 khu có nhiều loài sở hữu, icon chồng/dồn khít khó thấy hết. Sửa: `GardenLayout.kt` thêm `gardenGridSize(itemCount)` dùng chung bởi `computeGardenSlots` (định vị) và `GardenMapView` (tính chiều cao khung) — chặn trần cột ở 5 (`MAX_COLS`, icon không bị bóp hẹp ngang) nên dư ra thì lưới cao thêm thay vì hẹp cột; `GardenMapView.kt` tính `zoneHeight = maxOf(230dp, 96dp * số hàng)` — khung tự cao lên đủ chỗ cho MỌI loài sở hữu, không cắt/đè loài nào. Cuộn dọc toàn trang (đã có sẵn) chính là thao tác "kéo" để xem hết khu đã cao hơn — không cần thêm pan/zoom riêng. Verify tay: cấp thêm 26 loài qua Bubble Cheat + SQL trực tiếp cho khu Khu Rừng (12 loài), khung tự cao lên rõ rệt, cả 12 icon hiện đầy đủ, tách bạch, không chồng, không crash.
  - **Lỗi phụ phát hiện khi tạo dữ liệu test:** `debug.service.ts#grantSpecies` (Bubble Cheat "Ngẫu nhiên") chỉ tăng `hatchCount`, quên tăng `ownedCount` — sót lại từ khi T-106 tách 2 khái niệm này, khiến loài cấp qua cheat không hiện được ở Khu vườn/không bán được. Đã sửa `create`/`update` để tăng cả 2, khớp đúng pattern `recordHatch()` thật ở `collection.service.ts`. Dữ liệu cũ của `tester01` bị kẹt `ownedCount=0` từ trước khi sửa (gồm cả loài SSR/MYTHIC) đã được heal thủ công bằng SQL 1 lần cho riêng tài khoản test này (không đụng dữ liệu người chơi thật).
  - **Sửa vòng 2, sau phản hồi Dev1002 ("SSR và các loại khác vẫn chưa hiện đầy đủ" + "bổ sung yếu tố nhận biết khu"):**
    1. Nhãn tên khu ("🌲 Khu Rừng"...) có thể bị icon loài đè lên che mất chữ khi jitter ngẫu nhiên rơi đúng góc trên-trái — icon có kích thước dp cố định (không co theo mật độ lưới) nên dù đã chừa ô (0,0) cho nhãn, icon ở ô kề vẫn có thể trôi lấn sang do jitter. Sửa `GardenLayout.kt#computeGardenSlots`: chừa hẳn ô (0,0) khỏi `cellOrder` (không gán loài nào vào đó) + chặn cứng thêm 1 lớp theo toạ độ tuyệt đối (`nếu x<0.34 và y<0.22 thì đẩy y=0.24`) để không loài nào đè nhãn dù rơi vào ô nào.
    2. Đạo cụ trang trí (cây/sóng/hoa/bệ Thần thú) trước đó vẽ số lượng cố định theo vị trí phần trăm chiều cao 230dp gốc — khi khung giãn cao (sửa ở trên), đạo cụ giãn theo tỉ lệ nên thưa dần, phần dưới khu trông trống trải, khó nhận ra đang ở khu nào. Sửa `GardenTerrain.kt`: `GardenZoneBackground` nhận thêm `density` (= số hàng lưới của khu, tính bởi `gardenGridSize`), rải đạo cụ theo từng "dải" (band) suốt chiều cao thay vì cụm cố định gần đỉnh — FOREST thêm cây bụi rải theo dải; SEA thêm hình sóng cung nhỏ (```) dọc bờ ngoài đường sông có sẵn — rõ "biển" hơn chỉ 1 đường sông; PLANT thêm khóm cỏ; MYTHIC giữ bệ chính cố định gần đỉnh (tránh trông như nhiều đền) + rải thêm tia lấp lánh (`drawSparkle`) suốt phần còn lại. Verify tay: 15 loài FOREST + 6 SEA + 9 PLANT + 4 MYTHIC (đã heal ownedCount ở trên) hiện đầy đủ, tách bạch, nhãn khu không bị che, cả 4 khu rõ ràng "đọc" được là rừng/biển/vườn/điện thần — không crash.
  - **Sửa vòng 3, sau phản hồi Dev1002 ("Điện thần thú xấu và chưa có sang trọng tinh tế lắm"):** bệ Điện Thần Thú trước đó chỉ là 1 khối oval tô đặc 1 màu (`drawOval(color = sun, ...)`) — phẳng, không có chiều sâu. Sửa `GardenTerrain.kt#drawMythicProps`: hào quang đổi từ 2 vòng tròn phẳng sang `Brush.radialGradient` toả mượt; bệ vẽ 2 lớp (viền dưới đậm `ShrineGoldDeep` + mặt trên `Brush.verticalGradient` sáng→đậm) tạo hiệu ứng vòm nhẹ thay vì phẳng; thêm viền sáng mảnh (sheen) trên mép; thêm 4 viên ngọc trang trí (`drawGem`, hình thoi viền + lõi trắng) quanh bệ theo 4 hướng. Màu vàng kim riêng (`ShrineGoldDeep`/`ShrineGoldLight`) chỉ dùng cho bệ này, theo đúng tiền lệ `WaterColor` (T-112). Verify tay: bệ có chiều sâu rõ rệt, không còn phẳng 1 màu, 4 viên ngọc + tia sáng lấp lánh xung quanh hiện đúng, không crash.
  - **Sửa vòng 4, sau phản hồi Dev1002 ("Vùng biển thêm sóng biển" / "Vườn cây thêm hàng rào" / "Điện thần thú thêm yếu tố thần linh"):**
    1. **SEA** — trước chỉ có 1 dải sông uốn lượn, dễ nhầm "sông" hơn "biển". Thêm `drawWaveRow()` — hàng sóng cung lượn ngang chạy suốt bề rộng khu (khác đường sông chạy dọc), 2 lớp mỗi dải (mờ+đậm, biên độ xen kẽ) rải theo `bands` — đọc ra ngay "mặt biển gợn sóng" theo chiều ngang.
    2. **PLANT** — thêm `drawFenceRow()` (rào gỗ: 2 thanh ngang + cọc đứng cách đều) chạy suốt mép dưới khu (luôn ở đáy dù khu giãn cao) + 1 đoạn ngắn góc trên-phải (góc trên-trái đã có nhãn khu che, không rào ở đó cho đỡ phí) — gợi "1 khoảnh vườn được rào lại" thay vì bãi cỏ mở.
    3. **MYTHIC** — thêm 3 yếu tố "thần linh": `drawRadiance()` (12 tia hào quang toả quanh bệ), `drawGuardianPillar()` (2 cột đá/đèn lồng chầu 2 bên bệ, đỉnh có đốm sáng), `drawCloudWisp()` (2 vầng mây linh trôi gần đỉnh khu) — biến bệ đơn độc thành bố cục "điện thờ" đầy đủ hơn.
    Verify tay: cả 3 thay đổi hiện đúng trên emulator (sóng ngang rõ ràng ở Vùng Biển, rào gỗ ở mép dưới + góc Vườn Cây, tia sáng + 2 cột đá quanh bệ Điện Thần Thú), không đè lên nhãn khu, không crash.

- **T-114 — Android: âm thanh nền Khu vườn.** `ForestViewModel` inject `SoundManager` (T-042) + `TimerRepository` — vào chế độ Khu vườn gọi `playAmbientTrack("forest")` **chỉ khi không có phiên tập trung nào đang `RUNNING`** (tránh chồng nhạc lên ambient riêng của phiên), cờ `gardenAmbientActive` đảm bảo chỉ tự tắt nhạc do chính Khu vườn bật (không lỡ tắt nhạc phiên đang chạy độc lập). Tự dừng khi rời chế độ hoặc `onCleared()`.

- **T-116 — Backend + Android: sửa lại logic tỉ lệ nở trứng + thêm 3 trứng Truyền Thuyết (2026-07-26, theo yêu cầu Dev1002).**
  - **Bối cảnh:** Dev1002 định nghĩa lại luật tỉ lệ nở:
    - Trứng Rừng/Biển/Hoa: chỉ B-A-S (B cao, A trung bình, S thấp), **không bao giờ** ra Thần Thú.
    - Trứng Bí Ẩn: A→SSR (A cao, S thấp, SSR cực kỳ thấp), gồm tất cả loài mọi khu + Thần Thú.
    - **Mới:** 3 trứng Truyền Thuyết (Rừng/Biển/Hoa) — S cao, SSR trung bình — nở ra trọn khu + Thần Thú cùng chủ đề, ấp 6 giờ, hiện ở Cửa hàng nhưng **không giá/không mua được**, chỉ Admin phát làm phần thưởng. Toàn bộ tỉ lệ phải chỉnh được từ AdminJS.
  - **Giả định CHƯA được Dev1002 duyệt cụ thể, cần xem lại:**
    1. **Cấp SS không được nhắc tới ở bất kỳ loại trứng nào trong yêu cầu gốc** (chỉ B-A-S cho trứng thường, A-S-SSR cho Bí Ẩn, S-SSR cho Truyền Thuyết). Để không "kẹt" 17 loài cấp SS hiện có (không thể nở ra được từ đâu cả), Claude đã tự quyết định xếp SS vào 2 nhóm còn lại theo thứ tự giảm dần hợp lý: **Bí Ẩn** (A→S→SS→SSR, SS nằm giữa S và SSR) và **Truyền Thuyết** (S→SS→SSR). Trứng Rừng/Biển/Hoa giữ đúng CHỈ B/A/S như yêu cầu.
    2. **25 loài Thần Thú (MYTHIC) chưa từng được chia theo chủ đề Rừng/Biển/Hoa** — DB gốc coi cả 25 loài là 1 khối chung. Để 3 trứng Truyền Thuyết ra đúng "Thần Thú cùng chủ đề", Claude tự đề xuất chia theo 5 "họ" loài có sẵn (mỗi họ 5 con): **Kỳ Lân + Hồ Ly → Rừng** (10 con), **Long + Hạc → Biển** (10 con, rồng/hạc gắn với sông/mây trong dân gian), **Phượng Hoàng → Hoa** (5 con, lửa/ánh sáng gần khu vườn hơn). Xem `prisma/seed.ts` biến `mythicByFamily`/`mythicForest`/`mythicSea`/`mythicPlant` — đổi lại dễ dàng nếu Dev1002 muốn chia khác.
    3. **Con số trọng số cụ thể** (không phải tỉ lệ % cuối — xem bảng dưới) là Claude tự chọn để khớp mô tả định tính "cao/trung bình/thấp": Rừng/Biển/Hoa `B=70,A=25,S=5`; Bí Ẩn `A=65,S=22,SS=10,SSR=3`; Truyền Thuyết `S=55,SS=25,SSR=20`. Có thể chỉnh trực tiếp qua AdminJS bất cứ lúc nào, không cần sửa code.
  - **Schema (`prisma/schema.prisma`):**
    - Model mới `EggRarityWeight { eggTypeId, rarity, weight }` (khoá kép `@@id([eggTypeId, rarity])`) — thay hẳn `RarityWeight` toàn cục cũ (1 trọng số dùng chung mọi trứng). Cấp KHÔNG có dòng ở 1 loại trứng = trọng số 0 = không bao giờ rơi.
    - `ShopItem` thêm `purchasable Boolean @default(true)` — `false` = hiện ở Cửa hàng nhưng khoá nút mua.
    - Migration `20260726133309_legendary_eggs_and_per_egg_rarity_weights` — áp dụng bằng cách chạy trực tiếp `migration.sql` qua `docker exec psql` (không dùng `prisma migrate dev`, đúng quy ước cục bộ) vì `prisma migrate resolve --applied` chỉ đánh dấu lịch sử, KHÔNG tự chạy SQL — phải chạy SQL thủ công riêng, đã gặp lỗi này và tự sửa khi verify.
  - **`eggs.service.ts#loadWeightedDrops`:** đổi nguồn tra trọng số từ `prisma.rarityWeight.findMany()` (toàn cục) sang `prisma.eggRarityWeight.findMany({where:{eggTypeId}})` (riêng từng trứng); cấp thiếu dòng → `?? 0` rồi lọc `effectiveWeight > 0` (khác hành vi cũ `?? 1` mặc định trọng số 1 khi thiếu — đổi có chủ đích để "vắng mặt = không bao giờ rơi").
  - **`shop.service.ts#purchase`:** chặn thêm ngay đầu hàm nếu `shopItem.purchasable === false` → `ForbiddenException` — chặn cả phía server phòng client bỏ qua UI khoá nút (đã verify bằng curl: mua trứng Truyền Thuyết trả về 403 đúng thông báo).
  - **`prisma/seed.ts`:** thêm 3 `EggType` mới (`priceCoin/priceHours=0`, `hatchDurationMin=360`), drop table = trọn khu (50 loài, để `EggRarityWeight` tự lọc B/A ra) + slice Thần Thú theo chủ đề; `EggRarityWeight.createMany` cho đủ 7×(3-4 dòng); 3 `ShopItem` mới `purchasable:false`. Bảng `rollFromPool`/`rarityWeightMap` cũ (chỉ dùng để tạo LỊCH SỬ ẤP GIẢ cho 10 tài khoản tester demo, không phải logic thật) giữ nguyên không đổi — không ảnh hưởng đúng/sai của hệ thống thật.
  - **AdminJS (`admin.module.ts`/`admin-i18n.ts`):** đổi resource `RarityWeight` → `EggRarityWeight`; **`OwnedEgg` được mở khoá action `new`** (trước đây `readOnly` hoàn toàn, kể cả tạo mới) — đây chính là cách Admin "phát thưởng": vào resource OwnedEgg → New → chọn User + EggType (chọn 1 trong 3 Truyền Thuyết) → Save, tạo thẳng 1 `OwnedEgg` trạng thái `INCUBATING` cho user đó, ấp bình thường qua phiên tập trung như trứng mua. Vẫn khoá `edit`/`delete`/`bulkDelete` (không cho admin chỉnh tay tiến độ ấp/kết quả nở, phải qua API để giữ đúng nghiệp vụ).
  - **Android:** `ShopItemDto` thêm `purchasable: Boolean = true`; `ShopScreen.kt#ShopItemRow` khi `!purchasable` hiện dòng "Chỉ nhận qua thưởng từ Admin" thay giá + nút "Không bán" (disabled) thay "Mua ngay".
  - **Verify:**
    - Curl `GET egg-types/:id/odds` cho cả 7 loại trứng — khớp đúng thiết kế: Rừng B=81.4%/A=17.4%/S=1.2%; Biển B=74.8%/A=24.3%/S=1.0%; Hoa B=79.1%/A=18.4%/S=2.5%; Bí Ẩn A=83.0%/S=10.5%/SS=4.5%/SSR=2.0% (không B); Truyền Thuyết Rừng S=45.8%/SS=20.8%/SSR=33.3%; Truyền Thuyết Biển S=42.3%/SS=19.2%/SSR=38.5%; Truyền Thuyết Hoa S=62.3%/SS=25.2%/SSR=12.6% (không B/A) — không có SSR nào lọt vào Rừng/Biển/Hoa/Bí Ẩn quá tỉ lệ thiết kế.
    - Curl mua trứng Truyền Thuyết → 403 đúng thông báo; `GET shop-items?category=EGG` → `purchasable:false` đúng cho 3 trứng Truyền Thuyết, `true` cho 4 trứng còn lại.
    - Mô phỏng Admin phát thưởng: `INSERT` trực tiếp 1 `owned_eggs` (khớp đúng những gì AdminJS `new` form sẽ submit) cho tester02 với `eggTypeId` = Trứng Truyền Thuyết Rừng → `GET owned-eggs` trả về đúng, `hatchDurationMin=360`, `status=INCUBATING` — luồng ấp trứng thật (qua `EggPickerDialog`/phiên tập trung) sẽ hoạt động y hệt trứng mua thường vì dùng chung `OwnedEgg`/`Session` model.
    - Android: rebuild + cài lại + đăng nhập lại (re-seed xoá user cũ) → mở Cửa hàng qua FAB, xác nhận tay 3 trứng Truyền Thuyết hiện đúng "Chỉ nhận qua thưởng từ Admin" + nút "Không bán" (khoá), 4 trứng còn lại hiện giá + "Mua ngay" bình thường.
    - Artifact bảng tỉ lệ nở (đã gửi Dev1002) cập nhật lại đủ 7 loại trứng + 301 dòng, tổng % mỗi nhóm ≈100%, khớp số liệu curl ở trên.

- **T-117 — Android: 5 sửa nhỏ sau phản hồi Dev1002 (2026-07-26, cùng ngày T-116).**
  1. **Lỗi "che top view" — `EggPickerDialog.kt`.** Danh sách trứng sở hữu trước dùng `Column` + `forEach` KHÔNG giới hạn chiều cao — `Dialog` Compose mặc định wrap-content rồi tự căn giữa màn hình, nên khi sở hữu nhiều loại trứng (giờ có 7 loại + cheat cấp tự do, xem mục 5) danh sách đủ dài để đẩy `Dialog` cao hơn màn hình, phần trên tràn lên che vùng thanh trạng thái/thanh Cài đặt-số dư. Sửa: đổi `Column`+`forEach` sang `LazyColumn` có `Modifier.heightIn(max = 420.dp)` — danh sách dài giờ tự cuộn bên trong, `Dialog` không bao giờ cao vượt vùng an toàn dù sau này thêm bao nhiêu loại trứng. Verify tay: dialog hiện đúng, không tràn, đóng/mở lại bình thường.
  2. **Cấp SS chỉ xuất hiện ở Bí Ẩn/Truyền Thuyết** — đã đúng sẵn từ khi làm T-116 (xem mục đó, giả định 1), verify lại bằng curl `GET egg-types/:id/odds` cho cả Rừng/Biển/Hoa: chỉ trả về `["B","A","S"]`, không có `SS` — không cần sửa thêm.
  3. **Hiệu ứng nở "đặc biệt hơn" cho Bí Ẩn/Truyền Thuyết — `SessionResultDialog.kt#HatchedContent`.** Thêm `hatchTier(rarity)` suy thẳng từ cấp loài nở ra (SSR→2, SS→1, còn lại→0) — không cần biết loại trứng gốc vì sau T-116 chỉ Bí Ẩn/Truyền Thuyết mới cho ra SS/SSR. Theo tier: vòng tròn loài to dần (96dp→112dp→132dp) + nhịp đập mạnh/nhanh hơn (biên độ 1.06→1.1→1.14, chu kỳ 900ms→900ms→650ms); tier≥1 thêm `RadianceRays` (tia hào quang xoay quanh, Canvas vẽ trong `Box` kích thước CỐ ĐỊNH — không lặp lại lỗi tràn màn hình ở mục 1); tier=2 (SSR) thêm `SparkleOrbit` (lấp lánh xoay ngược chiều) + tiêu đề đổi hẳn ("🌟 Huyền thoại! Thần Thú xuất hiện!" so với "Chúc mừng! Trứng đã nở" mặc định). Build sạch, code review kỹ (Box/Canvas đều kích thước cố định, không unbounded); **chưa xác nhận trực quan được** trên emulator lần này do máy chủ quá tải (xem cuối mục) — tái dùng đúng kỹ thuật Canvas xoay/tỉa sáng đã xác nhận đẹp ở bệ Điện Thần Thú (T-113 vòng 3), rủi ro thấp.
  4. **Tên phiên bản sai trong Cài đặt** — `versionName` trong `build.gradle.kts` vẫn là `0.7.0` (bump lần cuối cho T-042/043, TRƯỚC khi làm toàn bộ Chợ/Khu vườn/Trứng T-106→T-117 trong phiên này) nên dòng "Phiên bản v{fallback}" ở local build hiện giá trị cũ, không phản ánh đúng khối lượng công việc mới. Logic đọc `BuildConfig.RELEASE_TAG` (ưu tiên tag CI thật, fallback `v{versionName}` khi build local) đã đúng từ trước, không phải bug logic — chỉ cần bump theo đúng quy ước dự án (mỗi đợt tính năng lớn bump 1 lần, xem lịch sử `versionCode` 2→6 các commit trước). Bump `versionCode` 7→8, `versionName` "0.7.0"→"0.8.0". Verify tay: Cài đặt hiện đúng "Phiên bản v0.8.0".
  5. **Bubble Cheat cấp trứng theo từng loại riêng biệt — `TesterCheatMenu.kt`/`TesterCheatViewModel.kt`.** Trước chỉ cấp được "Trứng Bí Ẩn" cố định (1 nút). Backend `debug/grant-egg` vốn đã nhận `eggTypeName` tự do (không cần sửa backend) — chỉ cần Android truyền đúng tên. Đổi `onGrantMysteryEgg: () -> Unit` thành `onGrantEgg: (String) -> Unit`, thêm danh sách `EGG_CHEAT_OPTIONS` (7 mục: Rừng/Biển/Hoa/Bí Ẩn/TT.Rừng/TT.Biển/TT.Hoa, tên PHẢI khớp tuyệt đối `EggType.name` ở `seed.ts`), mỗi mục 1 nút riêng trong lưới `LazyVerticalGrid`. Tách "Trứng (7 loại)" và "Loài ngẫu nhiên theo cấp" thành 2 lưới riêng, mỗi lưới `Modifier.height(...)` CỐ ĐỊNH (220dp/114dp) — áp đúng bài học ở mục 1: dù sau này thêm bao nhiêu tuỳ chọn, 2 lưới luôn tự cuộn bên trong, không đẩy `Dialog` tổng vượt màn hình. Verify tay: mở menu, thấy đủ 7 nút trứng + 5 nút cấp, dialog vừa khít màn hình không tràn; cấp thử "Trứng Biển" thành công (toast "Đã nhận: Trứng Biển").
  - **Sự cố khi verify (không phải bug code):** cuối phiên test tay, emulator gặp ANR liên tiếp ("CozyPomo isn't responding" rồi "Process system isn't responding") — kiểm tra `adb logcat` cho thấy nguyên nhân là **"Input dispatching timed out"** với tỉ lệ CPU kernel bất thường (17-22%) và `uptime` xác nhận load average host **13-20** (quá tải nghiêm trọng, có thể do cộng dồn nhiều tiến trình Gradle/Kotlin daemon + emulator + backend Node chạy đồng thời suốt phiên dài). Đây là quá tải TÀI NGUYÊN MÁY CHỦ, không phải deadlock trong code mới — đã `force-stop` + khởi động lại app sạch, không sửa code để "vá" hiện tượng này. Do sự cố này, mục 3 (hiệu ứng nở SS/SSR) chưa kịp xác nhận trực quan trước khi phiên làm việc phải dừng — cần Dev1002 tự bấm thử khi máy đỡ tải (chọn 1 trứng Truyền Thuyết đã ấp gần đầy qua cheat, hoàn thành phiên, xem hiệu ứng nở).

- **T-118 — Commit + push + release production (2026-07-26, theo yêu cầu Dev1002).**
  - Commit `15111cd` gộp toàn bộ phiên làm việc (T-042/043 SoundManager+WorkManager, T-106→T-114 Chợ+Khu vườn, T-116/T-117 kinh tế trứng 7 loại + 5 sửa lỗi) — 66 file, +4099/-174 dòng. Push thẳng lên `main` (branch làm việc và `main` đang cùng 1 commit gốc, không cần PR).
  - **Phát hiện + sửa 1 lỗi build release thật** (không lộ ra ở build debug đã test suốt phiên): `CozyPomoApp` cấp `Configuration.Provider` riêng cho WorkManager (T-043, để inject `HiltWorkerFactory`) nhưng chưa gỡ `androidx.startup.InitializationProvider` mặc định khỏi `AndroidManifest.xml` — `lintVital` (chỉ chạy ở build release, luôn coi là lỗi fatal) chặn đứng `app-v1.20260726.001` lần build đầu. Sửa bằng cách thêm `<provider>` gỡ `WorkManagerInitializer` theo đúng khuyến nghị chính thức của AndroidX — verify lại bằng `./gradlew :app:lintVitalRelease` + `:app:assembleRelease` chạy sạch cục bộ trước khi push lại (commit `f99cf15`).
  - **`backend-v1.20260726.001`** — release qua `gh release create`, trigger `backend-deploy.yml`: build+push Docker image → SSH `docker compose pull && up -d` lên aaPanel. Container tự chạy `prisma migrate deploy` trong `CMD` (đã có sẵn từ trước, không cần thêm bước migration riêng trong workflow). Sau khi deploy xong, verify `GET /api/v1/egg-types` trả về đúng 4 loại cũ (dữ liệu cũ, bảng mới trống) — đúng như dự kiến vì migrate chỉ tạo schema, chưa có data.
  - **Reseed production** (Dev1002 xác nhận cho phép "seed lại toàn bộ dữ liệu"): trigger `workflow_dispatch` trên `backend-deploy.yml` với `reseed=true confirm_reseed=RESEED` — xoá sạch + chạy lại `prisma/seed.ts` trên DB thật. Verify lại `GET /api/v1/egg-types` → đủ 7 loại trứng (gồm 3 Truyền Thuyết `priceCoin=0`) — xác nhận reseed đúng dữ liệu mới trên production thật.
  - **`app-v1.20260726.001`** (`v0.8.0`, versionCode 8) — release lần 1 fail vì lỗi WorkManagerInitializer ở trên; xoá release+tag hỏng (`gh release delete --cleanup-tag`), tạo lại cùng tên tag trỏ đúng vào commit đã sửa (`f99cf15`) — lần 2 build xanh, AAB+APK ký release đã đính kèm vào GitHub Release.
  - **Rủi ro đã cân nhắc trước khi làm:** release backend tự động deploy production qua SSH, không có bước xác nhận riêng trong CI (khác hẳn app release chỉ tạo file tải về) — đã hỏi rõ Dev1002 phạm vi (chỉ app / cả 2 kèm tự chạy migration sau / dừng lại) trước khi tạo `backend-v*`, và xác nhận migration đã tự động (container `CMD`) trước khi tiến hành, để tránh nguy cơ crash 500 ở endpoint Chợ/Trứng nếu bảng mới chưa tồn tại.

- **Đã verify:**
  - Backend: `npm run build` (sau khi xoá cache `tsconfig.build.tsbuildinfo` cũ gây thiếu module — lỗi môi trường cục bộ, không phải lỗi code) chạy sạch; migration áp dụng thật lên Postgres local (Docker); toàn bộ luồng Market test bằng `curl` thật với tài khoản tester thật (xem T-106).
  - Android: `./gradlew :app:compileDebugKotlin` và `./gradlew :app:assembleDebug` xanh hoàn toàn, bao gồm `hiltJavaCompileDebug` (đồ thị Dagger/Hilt của toàn bộ ViewModel mới — `MarketViewModel`, `ForestViewModel` thêm 2 dependency mới — hợp lệ).
  - **Đã test thao tác tay trên emulator** (`cozypomo_test`, `emulator-5554`): onboarding → đăng nhập → Trang chủ (FAB Chợ/Cửa hàng nổi đúng vị trí, hoạt ảnh mở/đóng mượt) → Chợ (tab "Tất cả"/"Của tôi" hiện tin đăng thật, đúng tên/giá/badge hiếm) → Khu rừng chế độ Lưới (thẻ loài hiện đúng `hatchCount` lịch sử dù đã bán hết `ownedCount`) → Khu rừng chế độ **Khu vườn** (toggle 2-icon hoạt động, cả 3 khu Khu Rừng/Vùng Biển/Vườn Cây vẽ Canvas đúng — cây, sông màu `--water`, cụm hoa — sinh vật sở hữu đặt đúng vị trí trong khu, không tràn màn hình, không chồng lấp, không crash).
  - **Lỗi phát hiện qua test tay + đã sửa:** tab "Của tôi" ở `MarketScreen.kt` hiện dòng "Đã bán · +40 Xu" dùng thẳng `listing.priceCoin` thô, trong khi người bán thực nhận chỉ 36 Xu sau phí 10% — gây hiểu lầm số Xu thực nhận. Sửa: thêm `marketFeePercent`/`maxActiveListingsPerUser` vào `GameSettingsDto`, `MarketViewModel` đọc thật `GET game-settings` (không hardcode 10%, vì Admin có thể đổi — đúng yêu cầu câu hỏi 4), `MarketListingRow` tính `Math.round(priceCoin * (1 - feePercent/100))` và hiện rõ cả số Xu thực nhận lẫn % phí. Đã rebuild + cài lại + verify lại đúng trên emulator.
  - Chưa spot-check hết: màn Cửa hàng qua FAB (đã build/route xong, chưa bấm thử tay), luồng dialog "Đăng bán" từ Chi tiết loài và từ Kho đồ (trứng), trạng thái nút "Cần thêm" khi không đủ Xu, hiển thị tin PENDING_APPROVAL cho loài SSR/MYTHIC ở tab "Của tôi".
- **Chưa commit/push** theo đúng yêu cầu Dev1002 ("triển khai xong mở emulator cho tôi kiểm tra, không push code") — emulator đang chạy sẵn app bản mới nhất để Dev1002 tự kiểm tra.

### T-119 — Thiết kế lại toàn bộ 175 loài: "hầm hố" rồi tinh chỉnh lại thành "có cá tính, vẫn dễ thương" (2026-07-27)

Dev1002 phản hồi thiết kế sinh vật hiện tại (thú rừng/sinh vật biển/thần thú) "rất xấu" — yêu cầu thiết kế lại toàn bộ, ban đầu theo hướng "hầm hố" (silhouette góc cạnh, gai/vuốt/mắt hí, tông tối). Sau khi xem bản pitch, Dev1002 phản hồi tông màu quá dữ — chỉnh lại theo hướng creature-collector hiện đại kiểu Coromon/Palia: giữ silhouette có cá tính nhưng đổi bảng màu sang sáng/bão hoà/tinh tế. 2 bản pitch (Artifact HTML, không phải code thật) dùng để chốt hướng trước khi port vào 2 codebase thật.

- **Bảng màu (14 palette, `SPECIES_PALETTE`/`PALETTE`)** — đổi toàn bộ từ tông pastel/đất (bản gốc) sang tông kẹo sáng bão hoà. Giữ nguyên cấu trúc 3 tông `base`/`dark`/`light` (không đổi tên field, tránh phải sửa lại mọi archetype đang dùng `p.light`) nhưng đổi vai trò `light` từ "pastel nhạt" thành **glow** (điểm nhấn sáng vui ở mắt/sparkle).
- **35 archetype** (10 forest + 10 sea + 10 plant + 5 mythic) — thêm đồng loạt: mắt to hơn kèm catchlight (thú rừng) hoặc mắt hí phát sáng màu glow (biển/thần thú), 1 mảng highlight sáng trên thân, 1 điểm sparkle ✦ lấp lánh mỗi loài. Riêng `cactus` (thân trụ góc cạnh + gai + hoa sao) và `dragon` (gai sống lưng + sừng đôi + vảy bụng sáng) vẽ lại hẳn theo bản pitch đã duyệt. **Tiện thể sửa 1 lỗi phát hiện khi rà code cũ:** archetype `fern` và `bamboo` trước đây vẽ hoàn toàn bằng màu cố định (`LEAF_BASE`/`LEAF_DARK`), bỏ qua `paletteIdx` — mọi loài fern/bamboo trông y hệt nhau bất kể palette gán. Đã sửa để 2 archetype này ăn màu theo palette như phần còn lại.
- **Hào quang rarity** (`rarityAura()`/`renderAura()`) — đổi ngôn ngữ hình ảnh từ "khói/tia điện/sét/lửa" sang "lấp lánh": B (viền chấm mờ) → A (2 viền ngọc lam) → S (4 sao vàng xoay) → SS (8 sao hồng-vàng xoay ngược chiều) → SSR (quầng gradient vàng-hồng + lõi trắng + 2 vòng nét đứt xoay ngược chiều + sparkle). Giữ nguyên 100% cơ chế animation cũ (Compose `breathe`/`spin`/`spinRev`/`flicker` ở Android, class CSS `sp-pulse`/`sp-aura-ring`/`sp-blaze`/`sp-flicker`... ở AdminJS) — chỉ đổi màu/hình vẽ, đảm bảo animation vẫn hiển thị đầy đủ ở mọi nơi loài xuất hiện (đã rà lại toàn bộ 6 call site `SpeciesArtIcon` trong Android + 2 component AdminJS, xác nhận đều truyền `rarity` và không tắt `animate`).
- **Rà soát trùng lặp kiểu + màu** — viết script kiểm tra toàn bộ 175 loài trong `seed.ts`, phát hiện 7 cặp loài trùng CẢ `archetype` lẫn `paletteIdx` (trông y hệt nhau): Cáo Buổi Sớm/Cáo Lá Phong (fox, idx7), Rùa May Mắn/Rùa Ngọc Bích (turtle, idx9), Cá Heo Sóng Nhẹ/Cá Heo Mưa Rào (dolphin, idx9), Sứa Đèn Lồng/Sứa Hồng Nhạt (jellyfish, idx4), Cá Ngựa Vằn/Cá Ngựa Sương Mai (seahorse, idx9), Hoa Tulip/Hoa Anh Đào Nhỏ (flowerRound, idx4), Dương Xỉ Sương Mai/Dương Xỉ Ngọc Bích (fern, idx9). Đổi `paletteIdx` của loài thứ 2 mỗi cặp trong `seed.ts` — xác nhận lại bằng script: 175/175 loài không còn cặp nào trùng archetype+palette.
- **Áp dụng lên dữ liệu đang chạy:** cập nhật `seed.ts` (nguồn khi reseed sau này) + `UPDATE` trực tiếp 7 dòng `species` tương ứng trên Postgres dev local (không reseed toàn bộ, tránh mất dữ liệu 10 tester đang có). **Lưu ý khi deploy production:** nếu không reseed production, 7 loài này trên production vẫn giữ `paletteIdx` cũ (trùng màu) cho tới khi có 1 trong 2: chạy `reseed-production` (xoá sạch dữ liệu thật, đã có cổng an toàn kép) hoặc `UPDATE` tay 7 dòng tương tự bản dev — **chưa tự ý làm cả 2 việc này trên production, chờ Dev1002 quyết định.**
- **Đã verify:** build sạch `./gradlew :app:compileDebugKotlin`/`:app:assembleDebug` và `npm run build` (backend); generate + render trực tiếp toàn bộ 175 loài qua chính `species-art.ts` thật (không phải mockup) thành 1 trang gallery, chụp bằng Chrome headless — soi bằng mắt xác nhận màu sắc tươi sáng, hào quang leo thang đúng cấp bậc, không có hình nào vỡ/lỗi, không có 2 loài nào trông giống hệt nhau.
- **Release (2026-07-27, theo yêu cầu Dev1002 "commit, release phiên bản cho tôi"):** bump Android `versionCode 8→9`/`versionName "0.8.0"→"0.9.0"`, verify `./gradlew :app:lintVitalRelease :app:assembleRelease` xanh cục bộ trước khi tag (rút kinh nghiệm từ sự cố T-118). Commit `2550b7e` → push thẳng `main`. Tạo `backend-v1.20260727.001` (kèm ảnh 175 loài render sẵn đính vào release note) → `backend-deploy.yml` build+push+SSH deploy production thành công, verify `GET /health` và `GET /species?category=FOREST` trả đúng dữ liệu mới. Tạo `app-v1.20260727.001` → `android-release.yml` build AAB+APK ký release thành công, đính kèm cả ảnh 175 loài vào release note. **Không reseed production** — 7 loài sửa `paletteIdx` ở mục trên vẫn giữ giá trị cũ trên production cho tới khi Dev1002 quyết định reseed hay update tay.

### T-120 — AdminJS: màn "Phát quà" — tặng thú/sinh vật biển/thần thú/trứng cho user theo ID/email/CSV (2026-07-27)

Dev1002 yêu cầu 1 màn admin cho phép tặng bất kỳ vật phẩm nào (loài các loại hoặc trứng) cho user theo ID, danh sách ID, hoặc file CSV — tặng nhiều item 1 lúc, hiện hình ảnh thật để chọn không nhầm.

- **`collection.service.ts`** — thêm `grantMany(userId, speciesId, quantity, tx?)`: cùng ngữ nghĩa `recordHatch` (tăng cả `hatchCount` lẫn `ownedCount`, coi như loài đã "mở khoá" thật cho user) nhưng cộng dồn `quantity` thay vì luôn +1, tách riêng khỏi `recordHatch` để không ảnh hưởng luồng tự ấp trứng hiện có.
- **`src/admin/components/GiftPage.tsx`** (mới) — trang AdminJS tuỳ biến hoàn toàn (không phải resource CRUD mặc định): 5 tab Rừng/Biển/Cây/Thần thú/Trứng, lưới thẻ hiện đúng ảnh SVG thật (tái dùng `SpeciesThumbnail`/`renderEggArt`+`renderEggAura` đã có ở `SpeciesList.tsx`/`EggTypeList.tsx` — không phải ảnh giả) + badge cấp bậc, bấm chọn nhiều item kèm ô số lượng riêng từng item, ô nhập người nhận (textarea ID/email, mỗi dòng 1 người hoặc phân cách dấu phẩy) + upload file CSV (1 cột ID/email), hiện kết quả sau khi phát (số người nhận thành công, danh sách không tìm thấy, lỗi nếu có).
- **`admin.module.ts`** — đăng ký page `gift-items` (label "Phát quà") với `handler` xử lý POST: gom token người nhận từ textarea + CSV (đọc file tạm qua `fs.readFileSync`, formidable đã có sẵn qua `express-formidable` dùng chung cho mọi route AdminJS), tách token giống UUID tra theo `id`, còn lại tra theo `email`, chạy vòng lặp user×item trong `$transaction` riêng từng lượt (1 lượt lỗi không chặn các lượt khác) — species gọi `collectionService.grantMany`, trứng gọi `ownedEggsService.create` lặp theo `quantity` (mỗi lần tạo 1 dòng `OwnedEgg` riêng, đúng logic 1 quả trứng ấp độc lập đã có).
- **Đã verify bằng `curl` thật** (đăng nhập admin lấy cookie session, `POST /admin/api/pages/gift-items` dạng multipart): tặng 1 loài (SSR MYTHIC) + 1 loại trứng, số lượng 2/3, cho 2 tester (1 theo ID, 1 theo email) — xác nhận đúng `hatchCount`/`ownedCount` loài +2 và số dòng `OwnedEgg` +3 ở cả 2 tester; test riêng đường CSV (3 dòng gồm 1 dòng tiêu đề "email" + 1 email không tồn tại) — đúng: bỏ qua tiêu đề, báo đúng email không tìm thấy, vẫn phát thành công cho 2 người còn lại; test 2 nhánh validate rỗng (không nhập người nhận / không chọn vật phẩm) đều trả lỗi rõ ràng, không crash.
- **Chưa release** — đã commit + push `main`, chưa tạo `backend-v` mới để deploy tính năng này lên production (chờ Dev1002 xác nhận).

### T-125 — Vật phẩm hỗ trợ (Boost items) + thưởng streak ngày liên tục 1-7 (2026-07-28)

Backend: `ShopCategory.BOOST` + `BoostType` (`FOCUS_MINUTES`|`HATCH_MINUTES`) trên `ShopItem`, 6 vật phẩm seed (Túi Thời Gian Nhỏ/Vừa/Lớn/Khổng Lồ +10/20/50/100 phút Giờ tích luỹ; Giọt Sương/Ánh Trăng Ấp Trứng +60/+300 phút ấp — item +300 `purchasable: false`, chỉ phát qua "Phát quà"). Giá hiện là **giá tạm** (Dev1002 sẽ chỉnh qua AdminJS sau). `PATCH inventory/:id/use` để dùng vật phẩm. `StreaksService` mới hook vào `SessionsService.complete()` — thưởng theo ngày 1-7 (không lặp vô hạn, streak đứt thì reset về ngày 1), cấu hình qua AdminJS resource `StreakRewardDay`, idempotent qua bảng `StreakClaim`. Android: `ShopScreen.kt` viết lại toàn diện — chạm vào hàng mở modal chi tiết (`ShopItemDetailDialog`), trứng mua thẳng bằng 1 trong 2 nút icon Xu Lá/Giờ tích luỹ (bỏ dialog "Trả bằng gì?" trung gian), hào quang cấp bậc (Thường/Bí ẩn/Truyền thuyết) quanh icon trứng, `StreakRewardDialog` hiện sau kết quả phiên, `ShopMarketToggleFab` thêm hiệu ứng "thở" thu hút chú ý lúc thu gọn.

- **Sự cố quy trình cần lưu ý:** toàn bộ việc trên được code trong 1 phiên trước ở worktree `support-items-streak-rewards-6074bd` nhưng **chưa từng được commit** — nằm im dưới dạng working-tree changes, không lên `main`, không cập nhật `plan.md`/memory ngay lúc đó. Một phiên khác (worktree `purchase-button-product-cards-51ef7e`, nhánh off `main`) sau đó cài bản build **không có** tính năng này lên cùng 1 emulator, khiến Dev1002 tưởng nhầm là code bị xoá mất. Đã xác minh build sạch (Android `compileDebugKotlin` + backend `nest build` + `prisma validate`) rồi mới commit vào nhánh `claude/support-items-streak-rewards-6074bd` (`b56d9cf`) — **vẫn chưa merge vào `main`**, chờ Dev1002 duyệt.
- **Bài học:** commit sớm hơn, đừng để việc lớn (37 file) nằm uncommitted qua nhiều phiên/worktree — dễ bị đè mất dấu vết khi có phiên khác build từ nhánh khác trên cùng thiết bị.

### T-126 — Đồng bộ màu nút giá Cửa hàng theo bubble số dư + gộp kiểu card 4 tab (2026-07-28)

Trên nền T-125 (vừa commit): nút giá trứng (`EggCurrencyButtons`/`MiniCurrencyButton`) trước đó cả Xu Lá lẫn Giờ tích luỹ đều tô cùng màu `primary` (xanh) nên khó phân biệt — sửa thành Xu Lá = `secondary` (vàng, khớp `BalancePill` ở bubble số dư) và Giờ tích luỹ = `primary` (xanh nhạt), áp dụng cả ở hàng danh sách lẫn modal chi tiết (`LargeCurrencyButton`). Bình thuỷ tinh/Nhạc nền/Vật phẩm hỗ trợ trước đó vẫn hiện dòng giá chữ + nút "Mua ngay" to bản riêng — đổi sang dùng chung 1 pill Xu Lá nhỏ gọn (`CoinOnlyButton`) như trứng, đồng nhất 4 tab. Giảm khoảng cách 2 nút trứng (6dp→4dp). Bump `versionName` "0.10.1" → "0.10.1a".

**Đưa nội dung mới của T-125 (lore 175 loài, 6 vật phẩm BOOST, 7 dòng streak_reward_days) lên production mà không đụng user thật:** `backend/prisma/seed.ts` xoá sạch toàn bộ dữ liệu (kể cả `nhininh410@gmail.com`) nên **không được chạy trên production** kể từ khi có tài khoản thật — job `reseed-production` trong `backend-deploy.yml` đã tự cảnh báo điều này. Viết thêm `backend/prisma/patch-boost-and-lore.ts` (chỉ INSERT vật phẩm BOOST còn thiếu theo tên + UPDATE `Species.lore` cho loài đang null + `createMany({skipDuplicates:true})` cho `streak_reward_days` — không `deleteMany` bất kỳ bảng nào) và job `patch-content` mới trong `backend-deploy.yml` (input `patch_content`, không cần gõ xác nhận vì không có rủi ro mất dữ liệu, `needs: deploy` để đảm bảo migration đã chạy trước). Đây là lựa chọn an toàn hơn hẳn "backup user rồi reseed toàn bộ rồi restore" — vì `Species`/`ShopItem`/`EggType` dùng `id` tự sinh UUID lúc tạo, reseed toàn bộ sẽ sinh ID mới, làm gãy toàn bộ FK (`CollectionEntry.speciesId`, `LedgerEntry.refShopItemId`, `OwnedEgg.eggTypeId`...) của **mọi** user hiện có, không riêng gì tài khoản cần giữ.

**✅ Đã release (2026-07-28):** merge `main` (`01bdd7f`) → `backend-v1.20260728.001` (build+push+deploy thành công, migration tự chạy qua `prisma migrate deploy` trong Dockerfile CMD) + `app-v1.20260728.001` (AAB/APK build/ký/upload thành công) → chạy tay `patch-content` qua `workflow_dispatch` (`deploy=true` để thoả `needs: deploy`, `patch_content=true`) — log xác nhận: cập nhật lore 175 loài, tạo 6 `ShopItem` BOOST, tạo 7 `StreakRewardDay`, không đụng bảng nào khác. Đã curl kiểm tra `cozyapi.nhutnm.id.vn` sống bình thường sau deploy.

### T-127 — Trứng Kapi + loài thứ 176 "Kapi Ngái Ngủ" (SSR, admin-only) (2026-07-28)

Dev1002 yêu cầu 1 trứng đặc biệt lấy cảm hứng từ 1 nhân vật game nổi tiếng (to mập, màu xanh dương, luôn ngủ) — **từ chối vẽ lại/sao chép thiết kế gốc vì đó là IP có bản quyền** (đã giải thích trực tiếp trong hội thoại), thay vào đó đề xuất 1 sinh vật **hoàn toàn nguyên bản** cùng tông màu/vóc dáng, dùng đúng hệ thống procedural sinh sẵn của game (archetype + palette, không phải asset ảnh rời) — Dev1002 duyệt hướng này qua bản xem trước (SVG mockup) trước khi code.

- Archetype mới **`sleepyGiant`** thêm vào `mythicSvg()` ([species-art.ts](backend/src/admin/components/species-art.ts)) và `mythicArt()` ([SpeciesArt.kt](app/app/src/main/java/com/cozypomo/app/ui/common/SpeciesArt.kt)) — port 1:1 như quy ước sẵn có của file. Thân to gấp rưỡi các archetype khác + mảng bụng `SHINE` lớn + tai nhỏ xíu (tôn vóc dáng khổng lồ), mắt nhắm (2 nét cong thay vì `eyeMark`/`seaEye` mở), chuỗi 3 vòng tròn mờ dần bay lên góc phải thay cho chữ "Zzz" thật (SVG `<text>` so với Compose Canvas không có glyph sẵn — khó giữ port 1:1 nếu dùng chữ thật). Dùng `paletteIdx: 3` (xanh dương `#6C9EFF`/`#3D5AC2` có sẵn trong bảng màu, không cần thêm màu thương hiệu mới).
- Loài **"Kapi Ngái Ngủ"** — `category: MYTHIC`, `rarity: SSR`, cố tình tạo **tách biệt** khỏi mảng `mythicNames`/`allSpecies` trong `seed.ts` (không thuộc 5 "họ" Thần Thú hiện có) để **không** rơi ngẫu nhiên ở Trứng Bí Ẩn hay bất kỳ trứng nào khác — chỉ nở từ đúng 1 trứng riêng.
- **Trứng Kapi** — `EggType` mới, màu `#4A5A82` (xanh xám trầm, cùng tông "huyền bí" như 3 Trứng Truyền Thuyết), `hatchDurationMin: 30`, `priceCoin`/`priceHours: 0`. 1 `EggDropEntry` + 1 `EggRarityWeight` (SSR, weight 100) — pool chỉ có đúng 1 loài nên luôn nở ra Kapi Ngái Ngủ 100%. `ShopItem` tương ứng `purchasable: false` — hiện trong AdminJS (Admin phát qua `OwnedEgg`, đúng pattern 3 trứng Truyền Thuyết), **không** hiện ở Cửa hàng.
- Tổng số loài: **175 → 176** (đã cập nhật đếm ở màn Khu rừng qua field tính toán sẵn, không cần sửa code — chỉ cần dòng dữ liệu mới).
- **Đưa lên production an toàn:** thêm bước 4 vào `patch-boost-and-lore.ts` (script additive-only đã dùng cho T-125) — chỉ `create` nếu Species "Kapi Ngái Ngủ" chưa tồn tại, hoàn toàn không đụng species/egg/user hiện có. Đã verify: build sạch (`compileDebugKotlin`, `nest build`), test trên local dev DB (seed đầy đủ + render thật qua Khu rừng, ảnh khớp mockup), test lại chính patch script đó ở chế độ "đã tồn tại" → đúng bỏ qua, không tạo trùng.

---

## ⬜ Chưa làm

### Nhóm A — Android app: xây từng màn hình thật (việc lớn nhất còn lại)

`HomeScreen.kt`, `ForestScreen.kt`, `ShopScreen.kt`, `StatsScreen.kt`, `SettingsScreen.kt` nay đều có UI/ViewModel/data layer thật — không còn màn nào dùng `PlaceholderScreen` trong 4 tab chính + Cài đặt (xem T-029→T-031, T-069, T-075, T-076, T-094, T-095 ở mục Đã hoàn thành).

- ~~T-029 Room DB local-first~~, ~~T-030 TimerRepository + Foreground Service~~, ~~T-031 Màn Trang chủ/Timer UI thật~~ — **đã xong**, xem mục Đã hoàn thành.
- ~~T-032 Popup chọn Trứng đầy đủ~~ — **đã xong, gộp vào T-069** (`EggPickerDialog.kt` hiện trứng sở hữu thật qua `GET /owned-eggs`, có thanh tiến trình ấp, không còn khái niệm "khoá theo mua" — mua qua Shop tạo `OwnedEgg` mới, không phải mở khoá 1 loại chung). Xem mục Đã hoàn thành.
- ~~T-033 Dialog xác nhận Bỏ cuộc~~ — **đã xong** (làm chung với T-031, xem `HomeScreen.kt`).
- ~~T-034 Hoạt ảnh nở trứng đầy đủ~~ — **đã xong, gộp vào T-069** (`SessionResultDialog.kt`: modal animate scale+fade, `HatchedContent` reveal loài + hào quang theo cấp bậc + đếm số Xu Lá).
- ~~T-035 Màn Khu rừng/Bộ sưu tập (S-04) UI thật~~ — **đã xong (T-075)**, xem mục Đã hoàn thành.
- ~~T-036 Chi tiết loài/Lore (S-03)~~ — **đã xong (T-075)**, xem mục Đã hoàn thành.
- ~~T-037 Màn Cửa hàng (S-05) UI thật~~ — **đã xong (T-076)**, mua trứng bằng Xu Lá hoặc Giờ tích luỹ đã hoạt động thật, xem mục Đã hoàn thành.
- ~~T-038 Màn Thống kê (S-06) UI thật~~ — **đã xong**, xem T-094 ở mục Đã hoàn thành.
- ~~T-039 Cài đặt (S-07) phần còn lại~~ — **đã xong, trừ S-07a chủ động bỏ theo yêu cầu Dev1002** (không phải hoãn — xem T-095 ở mục Đã hoàn thành). Cấu hình thời gian mặc định/Strict Mode/âm thanh + Kho đồ (equip bình-nhạc) đã có UI thật; Sao lưu & Đồng bộ Google Drive (`SyncRepository`) **không nằm trong phạm vi sản phẩm nữa** trừ khi Dev1002 yêu cầu lại.
- ~~T-040 Splash + Onboarding~~, ~~T-041 Đăng nhập/Đăng ký~~ — **đã xong**, xem mục Đã hoàn thành.
- ~~Refresh-token flow cho AuthRepository~~ — **đã xong (T-093)**, xem mục Đã hoàn thành — còn cần verify thật trên emulator (chỉ mới xác nhận build).
- ~~T-042 SoundManager/NotificationManager~~, ~~T-043 WorkManager outbox~~ — **đã xong (2026-07-25)**, xem mục Đã hoàn thành — chỉ mới verify qua Gradle build thật, **chưa verify trên emulator/thiết bị**.

### Nhóm B — Backend polish (nhỏ, không chặn deploy)

- **T-044 — Thêm `@ApiOperation({ summary })` cho từng endpoint.** Hiện Swagger/trang "API cho App" đang hiện tên hàm (`AuthController_login`) thay vì mô tả tiếng Việt dễ hiểu — cải thiện trải nghiệm người tích hợp app.
- **T-045 — Test suite backend.** Hiện 0 file `.spec.ts`. Ít nhất nên có unit test cho: rarity-roll (`egg_drop_table`/`rarity_weights`), currency ledger (không cho phép số dư âm), auth guard.

### Nhóm C — Test suite Android

- **T-046 — Test tầng logic.** Theo tech-spec đề xuất JUnit5 + Turbine (test Flow) cho Repository, đặc biệt `TimerRepository` (đếm giờ dễ lệch) và rarity-roll phía app nếu có logic client-side.
- **T-047 — Compose UI Test + Robolectric.** Cho các màn hình sau khi build xong ở Nhóm A.

### Nhóm D — Hạ tầng thật còn lại (việc của bạn, Claude không có quyền truy cập)

~~T-048 GitHub Secrets backend~~, ~~T-049 ghcr.io visibility~~, ~~T-050 aaPanel+Docker~~, ~~T-051 `.env` production~~, ~~T-052 Domain+SSL~~, ~~T-053 GitHub Secrets Android~~, ~~T-054 test workflow Android~~ — **đã xong**, xem T-059→T-066 và T-090 ở mục Đã hoàn thành.

### Nhóm E — Chuẩn bị phát hành Play Store

- ~~T-055 Feature graphic + ảnh chụp màn hình thật~~, ~~T-056 Chính sách bảo mật~~, ~~T-057 Data safety form (hướng dẫn)~~, ~~T-058 Content rating (hướng dẫn)~~ — **nội dung đã xong (T-103)**, xem mục Đã hoàn thành. Còn lại thuần thủ tục trong Play Console (không tự động hoá được — xem checklist trong `docs/play-store/README.md`): dán URL Privacy Policy (chờ release backend), điền 2 form theo hướng dẫn, upload assets, tạo app + chọn track.

### Nhóm F/G — Chợ (Marketplace) + Khu vườn của tôi — ĐÃ XONG, không phải việc chưa làm

Mục này từng liệt kê Chợ + chế độ xem Khu vườn cho Khu rừng là "đề xuất mới, chưa có dòng code nào" — **đã lỗi thời**. Cả 2 đã được thiết kế, code, verify, commit (`15111cd`) và release production (`backend-v1.20260726.001`/`app-v1.20260726.001`) trong cùng phiên 2026-07-26 — xem mục "Backend + Android — T-106→T-114" và "T-118" ở phần Đã hoàn thành phía trên để biết chi tiết đầy đủ (schema, luồng duyệt SSR, phí giao dịch 10%, giới hạn tin đăng, địa hình isometric-nhẹ Khu vườn, v.v.). Đã verify lại (2026-07-28): `MarketScreen.kt`, `GardenTerrain.kt`, `market.service.ts`, model `MarketListing` đều tồn tại thật trên `main`.

---

## Đề xuất thứ tự ưu tiên tiếp theo (cập nhật 2026-07-28)

Bản ghi cũ ở mục này đã lỗi thời (nhắc lại việc commit T-042/043 dù đã commit từ lâu, trích tag release cũ `app-v1.20260724.003`, lặp lại y hệt claim "Nhóm F/G chưa code" đã sửa ở trên). Trạng thái thật tính đến hôm nay: backend đang chạy `backend-v1.20260728.002`, Android đã release `app-v1.20260728.002` (T-127, loài thứ 176). Toàn bộ T-091→T-127 đã lên `main` + release production. Việc thật sự còn tồn đọng (đã verify lại bằng code, không phải suy đoán):

1. **Nhóm B — chưa có test suite backend** (T-044 `@ApiOperation` summary cho Swagger, T-045 unit test rarity-roll/ledger/auth guard) — verify lại: 0 file `.spec.ts`, 0 chỗ dùng `ApiOperation` trong toàn bộ `backend/src`.
2. **Nhóm C — chưa có test suite Android** (T-046 test tầng logic, T-047 Compose UI test) — verify lại: 0 file test (cả `test/` lẫn `androidTest/`).
3. **Nhóm E — Play Store**: nội dung xong, chỉ còn thao tác thủ công trong Play Console (dán URL Privacy Policy đã sống `200`, điền Data Safety/Content Rating, upload asset, tạo app + chọn track) — việc này ngoài quyền truy cập của Claude.
4. **Cân nhắc gỡ menu cheat tester (`TesterCheatMenu.kt`/`backend/src/debug/`) trước khi phát hành Play Store thật** — vẫn còn tồn tại trong code hiện tại, đã chặn server-side theo email tester nhưng vẫn là bề mặt debug không nên tồn tại vĩnh viễn.
5. **Dev1002 tự kiểm thử trên thiết bị thật** (không chỉ emulator) cho các luồng nhạy cảm với thời gian thực: refresh-token hết hạn thật (~15 phút), thông báo dự phòng khi mất mạng giữa phiên, outbox đồng bộ lại sau khi có mạng — các luồng này khó giả lập chính xác 100% trên emulator/CI.
